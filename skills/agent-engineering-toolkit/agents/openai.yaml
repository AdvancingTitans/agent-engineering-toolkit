interface:
  display_name: "Agent Engineering Toolkit"
  short_description: "Compatibility route for AET Quick and opt-in AET Lab."
  default_prompt: "Use $agent-engineering-toolkit only because I explicitly opted in for this task. Prefer one dedicated AET Quick Skill and stop after its result; enter legacy or Lab workflows only when I explicitly request them."
