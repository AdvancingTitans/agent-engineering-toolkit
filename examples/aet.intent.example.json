{
  "intent": "Add a focused validation rule with regression coverage.",
  "changed_path_budget": 12,
  "allowed_paths": [
    "aet.intent.json",
    "src/**",
    "tests/**",
    "docs/**",
    "README.md"
  ],
  "required_proofs": [
    {
      "id": "unit-tests",
      "command": "python -m unittest discover -s tests -v",
      "evidence": [
        "tests"
      ]
    }
  ]
}
