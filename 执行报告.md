# AET Evidence-Guided Planner 执行报告

日期：2026-07-29
工作区：`/Users/yjw/agent/Agent Engineering Toolkit`
目标版本：Python `1.17.0`；TypeScript `@aet/evidence-bundle` `0.2.0`
实施依据：`aet-evidence-guided-planner-delivery-package.md`
状态：Phase 0–6、真实案例、双语媒体与 GitHub Release 已完成；本次按规划顺序复验通过

## 1. 结论

规划包要求的 Evidence-Guided Planner 已按 Phase 0–6 顺序落地。最终
实现保持 AET 的 Evidence First 边界：AET 规范请求、构建受约束上下文、
校验宿主生成的严格 JSON Candidate、渲染可移植 Plan、导出只读 Skill，
并把外部 Diff 映射成待执行的验证交接；它不内置模型、不修改源代码、不
执行验证命令、不提升 Evidence 强度，也不授予合并或发布权限。

最终全仓安装态回归为 `536 tests / OK`。新增 Planner 代码总行覆盖率为
`90%`，核心 Validator 分支覆盖率为 `96%`。20 个冻结定位契约的
Planner Protocol 组通过全部 P1 指标门槛。Python Wheel、sdist、
TypeScript SDK、Host Skill、Schema、严格仓库审计和三个真实案例驱动的
E2E 场景均通过验收。另用真实 Codex `gpt-5.6-sol` 对同一 AET
跨模块任务分别执行 source-only、v1.16 evidence-only 和 v1.17 Plan
三组规划；Gold、原始结构化输出与评分器独立保存，不合成可信度总分。

## 2. 实际仓库路径映射

| 规划包逻辑职责 | 实际路径 | 映射说明 |
| --- | --- | --- |
| Planning Core | `src/aet/planning/` | 遵循现有 `src/aet/<domain>/` 布局 |
| CLI | `src/aet/cli.py` 的 `aet plan ...` | 遵循现有单一 CLI 注册器；Host 入口为 `skills/aet-plan/` |
| MCP | `src/aet/mcp_server.py` | 遵循现有 MCP 工具注册和分派方式 |
| Python SDK | `src/aet_bundle/planning.py` | 扩展现有轻量 `aet_bundle` 消费者包 |
| TypeScript SDK | `packages/evidence-bundle/runtime/planning.js`、`src/index.d.ts` | 复用现有可移植 Bundle/Atlas 消费包，避免重复包边界 |
| Planning Schemas | `schemas/planning/` | 遵循现有顶层协议 Schema 布局 |
| Host Skill | `skills/aet-plan/` | 新增薄的只读宿主 Skill |
| 文档 | `docs/evidence-guided-planner.md` 等 | 与现有高级能力文档并列 |
| E2E | `examples/evidence-guided-planner/` | 复用现有 Improvement 与 Atlas 示例输入 |
| Benchmark | `eval/evidence-guided-planner/`、`tests/fixtures/planning/` | 冻结 Gold 和独立可复算 Evaluator |

完整决策记录见
`docs/architecture/evidence-guided-planner-decision.md`。Harness Handbook
仅作为“先导航、再按需读取、保持当前源码复核和重同步”的产品理解参考，
没有形成代码、命名、网络或运行时依赖。

## 3. 最终架构与设计决策

权威数据流为：

```text
Natural-language Request
  -> deterministic Request normalization
  -> validated Bundle / Atlas adapters
  -> current-source navigation and hash binding
  -> bounded Planning Context + explicit gaps/omitted counts
  -> external Host Planner
  -> strict PlanCandidate JSON
  -> deterministic Candidate Validator
  -> integrity-bound portable Plan package
  -> read-only Helper / SDK / MCP / Skill export
  -> external code change
  -> Diff-to-pending-Proof verification handoff
```

主要决策：

1. `Evidence Bundle`、`Evidence Atlas`、当前源码、Proof 和 Freshness 仍是
   事实层；Plan 只拥有 `PROPOSED` 权威。
2. Host 负责模型推理，AET 不包含 API key、模型 SDK、模型路由或 Agent
   Loop。
3. Candidate 只接受无重复键的严格 JSON；自由文本、额外字段、非法枚举、
   绝对路径、`..`、软链接逃逸、保护路径和悬空引用均 fail closed。
4. REQUIRED 定位必须重新读取当前源码并绑定内容哈希；Atlas 历史路径不能
   直接替代当前源码事实。
5. 冲突、反证、Stale 和 `UNKNOWN` 原样保留。存在截断、关键缺口、未支持
   语言或高优先级冲突时，禁止宣称 `BOUNDED_COMPLETE`。
6. 所有预算截断都输出 omitted counts 和 `PARTIAL`，不静默丢弃。
7. Plan package 采用固定文件集和 SHA-256 Manifest；验证时拒绝缺失、
   多余、修改过或软链接化的内容。
8. Skill Export 只输出单个 Plan 所需的最小引用闭包，并拒绝秘密样式内容。
9. Verification Handoff 只解析外部 unified diff 并生成
   `UNKNOWN`/`PENDING` Proof 请求，不执行命令，也不修改原 Plan。
10. MCP 仅暴露八个结构化、工作区受限、只读 Planner 工具。

## 4. Phase 完成情况：实际 vs 规划

| Phase | 规划目标 | 实际结果 | 阶段测试 |
| --- | --- | --- | --- |
| 0 | 仓库侦察、契约冻结、路径映射 | 完成 ADR、真实接口映射、Bundle/Atlas/Improvement/CLI/MCP/SDK 边界确认 | 23 项文档/产品测试通过 |
| 1 | Schema、模型、策略、确定性校验内核 | 完成 6 个 Schema、状态/枚举、错误码、严格 Candidate Parser、Policy 和 Validator | 12 项通过 |
| 2 | Bundle/Atlas/Source 上下文构建 | 完成请求规范化、Bundle/Atlas 适配、相关性、源码导航、缺口检测、上下文构建 | 16 项通过 |
| 3 | CLI、计划包、Helper | 完成 9 个 `aet plan` 子命令中的 Context/Candidate/Package/Helper 能力及完整可移植包 | 5 项集成测试通过 |
| 4 | Host Skill、MCP、SDK | 完成 `aet-plan`、Skill Export、8 MCP、Python SDK、TypeScript SDK | Python 20 项、TypeScript 19 项通过；Skill 校验通过 |
| 5 | 执行后验证交接 | 完成安全 unified diff 解析、改动分类、回归谱系和待 Proof 交接 | 7 项通过 |
| 6 | Benchmark、E2E、文档、版本、发布门禁 | 完成 20 Gold、真实 Codex 三组对比、3 场景 E2E、双语文档、GIF/PNG/SVG/视频、CHANGELOG、包验收 | 最终全仓 536 项通过 |

## 5. 公共接口

### CLI

```text
aet plan context
aet plan validate-candidate
aet plan validate
aet plan show
aet plan explain
aet plan trace
aet plan gaps
aet plan export-skill
aet plan verification-handoff
```

### Python

`aet.planning` 稳定导出：

```text
PlanStatus
PlanningBudgets
PlanningContext
PlanningRequest
ValidationResult
build_planning_context
parse_candidate
validate_plan_candidate
build_plan_package
validate_plan_package
load_plan
show_plan
explain_edit
trace_path
list_gaps
export_plan_skill
validate_exported_skill
build_verification_handoff
build_verification_handoff_from_package
```

`aet_bundle` 便捷消费接口：

```text
load_plan
query_plan_edits
explain_edit
validate_plan
render_planner_context
build_verification_handoff
```

### TypeScript

`@aet/evidence-bundle` 新增：

```text
loadPlan
queryPlanEdits
validatePlanReferences
```

以及 `EvidenceLinkedPlan`、Edit、Reference、Gap、Verification 等只读类型。

### MCP

```text
aet_plan_build_context
aet_plan_validate_candidate
aet_plan_get
aet_plan_explain_edit
aet_plan_trace_reference
aet_plan_list_gaps
aet_plan_export_skill
aet_plan_build_verification_handoff
```

### Protocol

```text
planning-request/1.0
planning-context/1.0
plan-candidate/1.0
evidence-linked-plan/1.0
plan-reference/1.0
plan-manifest/1.0
```

Plan 状态、Edit disposition 和引用强度与规划包完全一致。

## 6. Changed files

以下是本次实现实际修改或新增的文件。用户在任务开始前已有的
`AGENTS.md`、交付规划包和 dark-luxury 视觉资产未被修改，不计入实现文件。

### 版本、注册与产品文档

| 文件 | 职责 |
| --- | --- |
| `CHANGELOG.md` | 新增 `1.17.0 — Unreleased` 说明和权威边界 |
| `PROJECT_MEMORY.md` | 记录 v1.17 实施状态、验收证据和恢复点 |
| `README.md` | 英文 Planner 产品入口、边界和高级文档链接 |
| `docs/README.zh-CN.md` | 中文 Planner 产品入口、边界和高级文档链接 |
| `pyproject.toml` | Python 版本、描述和 Planning Schema 包数据 |
| `uv.lock` | 锁定本地包版本 `1.17.0` |
| `src/aet/__init__.py` | Python 版本 `1.17.0` |
| `src/aet/cli.py` | 注册并处理全部 `aet plan` 子命令 |
| `src/aet/mcp_server.py` | 注册并处理 8 个 Planner MCP 工具 |
| `src/aet_bundle/__init__.py` | 导出 Python Planning SDK |
| `tests/test_mcp_server.py` | 冻结包含 Planner 的 MCP 工具集合 |
| `tests/test_quick_docs.py` | 保证 Planner 文档不污染四个 Quick 默认入口 |

### Planning Core

| 文件 | 职责 |
| --- | --- |
| `src/aet/planning/__init__.py` | 稳定公共 API |
| `src/aet/planning/errors.py` | 规划错误码和结构化异常 |
| `src/aet/planning/models.py` | 状态、枚举、请求、上下文、候选、计划和规范化序列化模型 |
| `src/aet/planning/schemas.py` | 发现并严格校验 Planning Schema |
| `src/aet/planning/candidate_parser.py` | 无重复键的严格 Candidate JSON 解析 |
| `src/aet/planning/policy.py` | 路径、保护范围、预算和完整性策略 |
| `src/aet/planning/validator.py` | Candidate 的确定性、fail-closed 核心校验 |
| `src/aet/planning/request_normalizer.py` | 自然语言请求和显式覆盖项规范化 |
| `src/aet/planning/bundle_loader.py` | Bundle 验证、身份绑定和规划记录适配 |
| `src/aet/planning/atlas_adapter.py` | Atlas Graph/Perspective 只读适配 |
| `src/aet/planning/relevance.py` | 确定性相关性排序 |
| `src/aet/planning/source_navigator.py` | 当前源码路径、符号、行范围和哈希复核 |
| `src/aet/planning/gap_detector.py` | 冲突、Stale、未知、截断和证据缺口检测 |
| `src/aet/planning/context_builder.py` | 构建预算受限的 Planning Context |
| `src/aet/planning/renderer.py` | 权威 JSON 的 Markdown 投影 |
| `src/aet/planning/package_builder.py` | 原子构建和完整性校验可移植 Plan package |
| `src/aet/planning/helper.py` | show/explain/trace/gaps 确定性只读查询 |
| `src/aet/planning/skill_exporter.py` | Codex/Claude Code/Generic 单 Plan 最小 Skill 导出 |
| `src/aet/planning/handoff.py` | 外部 Diff 到待 Proof 验证交接 |

### Schema

| 文件 | 职责 |
| --- | --- |
| `schemas/planning/planning-request-v1.schema.json` | Planning Request |
| `schemas/planning/planning-context-v1.schema.json` | Planning Context |
| `schemas/planning/plan-candidate-v1.schema.json` | Host Plan Candidate |
| `schemas/planning/evidence-linked-plan-v1.schema.json` | 已校验 Plan |
| `schemas/planning/plan-reference-v1.schema.json` | 可解析引用记录 |
| `schemas/planning/plan-manifest-v1.schema.json` | 可移植包 Manifest |

### Host Skill 与文档

| 文件 | 职责 |
| --- | --- |
| `skills/aet-plan/SKILL.md` | 触发条件、调用顺序、显示和停止规则 |
| `skills/aet-plan/references/contract.md` | 请求/候选/计划契约 |
| `skills/aet-plan/references/planner-protocol.md` | Host 严格 JSON 生成协议 |
| `skills/aet-plan/references/authority-boundary.md` | 只读、`PROPOSED`、不执行边界 |
| `skills/aet-plan/references/examples.md` | READY 与 NEEDS_EVIDENCE 示例 |
| `docs/architecture/evidence-guided-planner-decision.md` | Phase 0 ADR 和路径映射 |
| `docs/evidence-guided-planner.md` | CLI、Skill、MCP、SDK、包格式和边界完整文档 |
| `docs/planner-helper-scenarios.md` | Planner/Helper/Audit/Proof/Freshness 场景区分 |
| `docs/schemas/planning.md` | 六个 Schema 的关系和兼容规则 |
| `docs/assets/evidence-guided-planner-architecture.mmd` | Mermaid 架构源 |

### Python 与 TypeScript SDK

| 文件 | 职责 |
| --- | --- |
| `src/aet_bundle/planning.py` | Python 便捷消费和交接 API |
| `packages/evidence-bundle/package.json` | TypeScript 包版本 `0.2.0` |
| `packages/evidence-bundle/runtime/index.js` | 导出 Planning runtime |
| `packages/evidence-bundle/runtime/planning.js` | Plan 严格加载、查询和引用校验 |
| `packages/evidence-bundle/src/index.d.ts` | Planning TypeScript 类型和声明 |
| `packages/evidence-bundle/test/planning.test.mjs` | TypeScript Planning API 测试 |
| `packages/evidence-bundle/test/dist.test.mjs` | 构建产物 Planning 导出 smoke |

### Benchmark 与 E2E

| 文件 | 职责 |
| --- | --- |
| `tests/fixtures/planning/localization-gold.json` | 20 个冻结定位契约和三组独立预测 |
| `eval/evidence-guided-planner/run_benchmark.py` | 不调用 Planner 的确定性 Evaluator |
| `eval/evidence-guided-planner/results/v1.17.0.json` | 可字节复现的冻结 v1.17 结果 |
| `eval/evidence-guided-planner/README.md` | 数据集、指标、复现和局限 |
| `examples/evidence-guided-planner/build_example.py` | 两个现有案例驱动的三场景完整链路 |
| `examples/evidence-guided-planner/README.md` | E2E 复现说明 |
| `examples/evidence-guided-planner/frozen-candidates/single-file.json` | 单文件候选 |
| `examples/evidence-guided-planner/frozen-candidates/cross-module.json` | 跨模块候选 |
| `examples/evidence-guided-planner/frozen-candidates/needs-evidence.json` | 证据不足候选 |

### Python 测试

`tests/__init__.py`、`tests/planning/__init__.py` 和
`tests/integration/__init__.py` 提供测试包边界。

| 文件 | 覆盖职责 |
| --- | --- |
| `tests/planning/test_models.py` | 模型、状态、稳定身份和规范化 JSON |
| `tests/planning/test_schemas.py` | 六个严格 Schema |
| `tests/planning/test_policy.py` | 路径、保护范围和完整性策略 |
| `tests/planning/test_validator.py` | Validator 主路径和错误 |
| `tests/planning/test_validator_branches.py` | Validator 高分支覆盖 |
| `tests/planning/test_candidate_parser_edges.py` | 重复键、自由文本、空命令和非法形状 |
| `tests/planning/test_request_normalizer.py` | 请求规范化和高风险完整性词 |
| `tests/planning/test_bundle_loader.py` | Bundle 身份、引用和 Stale |
| `tests/planning/test_atlas_adapter.py` | Atlas 视图和不可越权语义 |
| `tests/planning/test_relevance.py` | 稳定相关性排序 |
| `tests/planning/test_source_navigator.py` | 当前源码、符号、大小写和软链接边界 |
| `tests/planning/test_gap_detector.py` | 冲突、未知、截断和缺口 |
| `tests/planning/test_context_builder.py` | 预算、omitted counts 和确定性上下文 |
| `tests/planning/test_renderer.py` | Markdown 投影和权威声明 |
| `tests/planning/test_package_builder.py` | 包文件集、Manifest、篡改和原子写入 |
| `tests/planning/test_skill_exporter.py` | 三种目标、最小引用、秘密和覆盖拒绝 |
| `tests/planning/test_python_sdk.py` | Python SDK |
| `tests/planning/test_mcp_planning.py` | 8 个 MCP 的结构化只读边界 |
| `tests/planning/test_handoff.py` | planned/unplanned/test-only/rename/Stale/回归谱系/不变性 |
| `tests/planning/test_prompt_injection.py` | 数据内伪指令、越权和 Candidate 注入 |
| `tests/planning/test_performance.py` | Context、100 Edit 校验、包体和时延预算 |
| `tests/planning/test_benchmark.py` | Gold 类别、非自评和 P1 门槛 |
| `tests/integration/test_plan_cli.py` | CLI Context→Candidate→Package→Helper |
| `tests/integration/test_planner_e2e.py` | 三个真实案例驱动场景 |

本文件 `执行报告.md` 刷新为本次用户要求的最终执行记录。

## 7. 测试、校验与真实结果

### 分 Phase 测试

| 阶段 | 执行命令/测试集合 | 真实结果 |
| --- | --- | --- |
| 基线 | `uv run --no-editable python -m unittest discover -s tests` | 478 tests，OK，68.862s |
| Phase 0 | 现有文档、Quick 产品面与边界测试 | 23 passed |
| Phase 1 | `test_models.py test_schemas.py test_policy.py test_validator.py` | 12 passed |
| Phase 2 | Request/Bundle/Atlas/Relevance/Source/Gap/Context 测试 | 16 passed |
| Phase 3 | Renderer/Package/CLI 集成测试 | 5 passed |
| Phase 4 | Skill Export/Python SDK/MCP/Prompt Injection 测试 | 20 passed |
| Phase 4 TS | `npm run build && npm test && npm run check` | 19 passed；compatibility guard 通过 |
| Phase 5 | `tests/planning/test_handoff.py` | 7 passed |
| Phase 6 | `pytest tests/planning tests/integration tests/test_quick_docs.py tests/test_mcp_server.py tests/test_python_bundle_sdk.py --durations=15` | 66 passed，6.56s |

### 最终回归与覆盖率

| 命令 | 真实结果 |
| --- | --- |
| `uv sync --reinstall-package agent-engineering-toolkit --no-editable && uv run --no-editable python -m unittest discover -s tests` | 安装 `1.17.0`；536 tests，OK，96.380s |
| `uv run --with coverage coverage run --source=src/aet/planning -m pytest tests/planning tests/integration -q` | 54 passed、62 subtests；1703 statements / 167 missed；总行覆盖率 90% |
| `uv run --with coverage coverage run --branch --source=aet.planning.validator -m pytest tests/planning/test_validator.py tests/planning/test_validator_branches.py -q` | 11 passed、10 subtests；110 branches；Validator 分支覆盖率 96% |
| `PYTHONPATH=src uv run --no-editable pytest tests/planning/test_benchmark.py tests/integration/test_planner_e2e.py -q` | 2 passed、3 subtests，2.60s |

### Benchmark、E2E、Schema、Skill 与发布包

| 命令 | 真实结果 |
| --- | --- |
| `PYTHONPATH=src uv run --no-editable python eval/evidence-guided-planner/run_benchmark.py` | `status=PASS`，20 fixtures |
| Benchmark stdout 与 `eval/evidence-guided-planner/results/v1.17.0.json` 的 `cmp` | 字节一致 |
| `uv run --no-editable python eval/evidence-guided-planner/run_real_case.py --output <temp>` | 三组真实 Codex 观察重新评分成功 |
| Real-case 输出与 `results/v1.17.0-real-codex.json` 的 `cmp` | 字节一致 |
| `PYTHONPATH=src uv run --no-editable python examples/evidence-guided-planner/build_example.py --output <temp>/output` | `status=PASS`；single-file READY、cross-module READY、needs-evidence NEEDS_EVIDENCE；全部验证 UNKNOWN |
| `find ... -name '*.json' ... \| xargs python3 -m json.tool` | 全部 Planning Schema、Fixture、Eval JSON 通过 |
| `uv run --with pyyaml --no-project python .../skill-creator/scripts/quick_validate.py skills/aet-plan` | `Skill is valid!` |
| `uv run --no-editable aet audit . --strict --format json --output <temp>` | exit 0；FAIL 0、UNKNOWN 0 |
| `npm run build && npm test && npm run check` | 19 tests passed；类型声明构建和兼容性守卫通过 |
| `uv build --out-dir <temp>` | Wheel 与 sdist 成功构建 |
| `unzip -t <wheel>` | `No errors detected` |
| 隔离 Wheel：`aet --version` | `1.17.0` |
| 隔离 Wheel：导入 `aet.planning`、`aet_bundle` | 成功；公开 `PlanStatus` 和 SDK |
| 隔离 Wheel：`aet plan --help` | 显示全部 9 个子命令 |
| Wheel/sdist 内容检查 | 包含 19 个 Planning 模块、Python SDK 和 6 个 Planning Schema |
| `git diff --check` | 通过 |

没有配置独立的 Python Lint 或静态 Type Checker；仓库现有验收面使用
unittest/pytest、JSON Schema、Skill Validator、npm build/test/check、
strict audit 和 build smoke。本次没有擅自引入新工具。

### 执行中出现并已纠正的失败

1. 第一次总覆盖率测量为 82%、Validator 为 81%，未达门槛；只补充了规划内
   边界/分支测试，并修复“空验证命令数组未拒绝”这一输入校验缺口。最终为
   90% / 96%。
2. 一次 Coverage 命令把单文件 `--source` 写成文件路径，Coverage 报告
   `No data was collected`；改用模块名 `aet.planning.validator` 后通过。
3. 第一次隔离 Wheel smoke 误引用未公开的 `PLAN_STATUS_READY` 常量；
   正式 API 是 `PlanStatus.READY_FOR_HUMAN_REVIEW`。改正验收命令后通过，
   无生产代码变更。
4. 第一次冻结 Benchmark `cmp` 发现结果文件是人工压缩视图而非 Evaluator
   的完整规范化 stdout；已把冻结结果更新为 Evaluator 的原样输出，随后
   字节复现通过。
5. 一次 Skill/JSON 校验误在 TypeScript 子目录执行，路径找不到，且直接
   Python 缺少 PyYAML；回到仓库根目录并用临时 `uv --with pyyaml` 后通过。

## 8. 定位质量指标

Gold Set 包含精确的 20 个冻结契约：

```text
single_file_behavior 4
cross_module_behavior 5
schema_runtime 3
implementation_test 3
config_docs 2
protected_path 1
stale_evidence 1
unresolved_conflict 1
```

Planner Protocol 组：

| 指标 | 门槛 | 实测 | 结果 |
| --- | ---: | ---: | --- |
| Required path recall | ≥ 0.90 | 1.0000 | PASS |
| Recommended path precision | ≥ 0.80 | 0.9706 | PASS |
| Required test recall | ≥ 0.85 | 1.0000 | PASS |
| Critical linkage omission rate | ≤ 0.10 | 0.0000 | PASS |
| Ungrounded/unsupported path rate | ≤ 0.05 | 0.0294 | PASS |
| Protected path rate | = 0 | 0.0000 | PASS |
| Conflict preservation rate | = 1 | 1.0000 | PASS |
| Unknown preservation rate | = 1 | 1.0000 | PASS |
| Reference resolution rate | = 1 | 1.0000 | PASS |
| Plan explainability rate | 辅助项 | 1.0000 | 记录 |

对照组：

| 组 | Path recall | Path precision | Test recall | Ungrounded | Protected | Reference resolution |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Source only | 0.6250 | 0.8750 | 0.3750 | 0.1250 | 0.0417 | 0.5417 |
| Bundle | 1.0000 | 1.0000 | 0.8750 | 0.0000 | 0.0000 | 1.0000 |
| Planner Protocol | 1.0000 | 0.9706 | 1.0000 | 0.0294 | 0.0000 | 1.0000 |

Token 和 duration 是冻结契约中记录的辅助观测，不是本次调用真实模型得到的
运行指标，不应解释为模型性能结论。

### 真实 Codex 单案例

任务要求定位 Graph Builder、固定 Perspective、递归 Viewer、Bundle 兼容层和
聚焦测试之间的联动修改范围。每组运行一次，不生成综合分：

| 指标 | Source only | v1.16 evidence-only | v1.17 validated Plan |
| --- | ---: | ---: | ---: |
| 必要生产路径召回率 | 1.0000 | 1.0000 | 1.0000 |
| 生产路径判断精确率 | 0.4444 | 1.0000 | 1.0000 |
| Disposition 准确率 | 0.7500 | 0.5000 | 1.0000 |
| 必要测试召回率 | 1.0000 | 0.0000 | 1.0000 |
| 证据引用覆盖率 | 0.0000 | 1.0000 | 1.0000 |
| 源码定位覆盖率 | 1.0000 | 1.0000 | 1.0000 |
| 联动覆盖率 | 0.3333 | 1.0000 | 1.0000 |
| `UNKNOWN` 保留率 | 0.0000 | 0.5000 | 1.0000 |

相对 source-only，v1.17 的路径判断精确率提升 55.56 个百分点、引用覆盖
提升 100 点、联动覆盖提升 66.67 点、`UNKNOWN` 保留提升 100 点；
必要路径和测试召回没有提升，因为对照组已经为 100%。第一次无约束
source-only 搜索进入 vendored/minified 资产并超出任务预算，被作为 harness
失败排除；正式观察为三组统一源码根目录和 generated/vendor 排除后的重跑。
该结果只支持这个真实案例的分项判断，不是通用 Agent 质量声明。

## 9. 与规划的差异点及原因

1. 推荐路径中的独立 CLI/MCP 子目录映射到现有 `src/aet/cli.py` 和
   `src/aet/mcp_server.py`。原因是仓库采用集中注册器；拆分会构成规划外
   重构。逻辑职责和接口完整保留。
2. 用户入口采用 Host Skill `aet-plan` 和 CLI `aet plan ...`，而不是在
   shell 中实现字面 `/aet-plan`。斜杠形式属于 Host 调用语法；CLI 遵循
   仓库现有子命令规范，能力等价。
3. TypeScript API 加入现有 `@aet/evidence-bundle`，没有创建新的
   `@aet/evidence-plan`。Phase 0 确认该包已承担 Bundle/Atlas 的可移植
   消费边界；新增三个只读 Plan 消费函数不会污染事实生产职责。
4. 没有新增 `aet plan create` 或内置 Planner。规划包最终强制接口是
   Context 构建和 Candidate 校验；模型生成必须留在 Host，避免扩大成
   Agent Runtime。
5. Gold Set 是独立于被测 Planner 编写且已冻结的契约，
   `generated_by_planner_under_test=false`。本次执行无法提供第二位人工
   审阅者签名，因此“独立人工复核”仍为 `UNKNOWN`，没有夸大为已完成。
6. 规划中的“增量计算引用复用率”没有独立公共接口或验收公式。为避免新增
   规划外缓存/状态层，本次只实现并测量确定性身份复用和时延/包体预算；
   计算复用率保留为 `UNKNOWN`。
7. 通用项目约定要求每阶段 commit/tag，但交付包明确要求未获授权不得提交、
   推送或创建 PR。本次遵循更具体的任务指令，因此只更新
   `PROJECT_MEMORY.md`，不执行 Git 写入远端或历史操作。

除上述仓库布局映射和明确 `UNKNOWN` 外，没有删除规划接口、放宽错误处理或
添加规划外产品功能。

## 10. 剩余风险与 UNKNOWN

- 20 个 Gold 是冻结、可复算的合成契约，不代表所有真实语言、构建系统或
  超大仓库；泛化质量为 `UNKNOWN`。
- 已完成一次真实 Codex 定位观察；跨任务 `pass@1`、`pass^3`、Plan 后实际
  实现成功率、用户理解率和人工审阅时长仍为 `UNKNOWN`。
- Gold Set 没有第二位人工审阅者的可验证签名，独立人工共识为 `UNKNOWN`。
- 计算引用的增量复用率未实现独立测量，仍为 `UNKNOWN`。
- MCP 工具已做协议和本地分派测试，但未在远程/真实 Host 连接中运行；
  连接层行为为 `UNKNOWN`。
- 性能测试覆盖源代码上下文 `<5s`、100 Edit 校验 `<2s`、渲染/打包
  `<2s` 和包体 `≤10MB` 的本地合成场景；真实超大仓库性能为 `UNKNOWN`。
- exact-tag CI Run `30435701149` 与 GitHub Release Run `30435886299`
  已通过；PyPI 发布不在本次范围。
- 工作区保留用户原有未跟踪文件；在人工提交前需要明确区分本次实现文件和
  用户资产。

## 11. 人工复现端到端命令

在仓库根目录执行：

```bash
uv sync --reinstall-package agent-engineering-toolkit --no-editable
uv run --no-editable python -m unittest discover -s tests

output_parent="$(mktemp -d)"
PYTHONPATH=src uv run --no-editable python \
  examples/evidence-guided-planner/build_example.py \
  --output "$output_parent/planner-e2e"

PYTHONPATH=src uv run --no-editable python \
  eval/evidence-guided-planner/run_benchmark.py

uv run --no-editable aet plan validate \
  "$output_parent/planner-e2e/single-file/plan"
uv run --no-editable aet plan show \
  "$output_parent/planner-e2e/single-file/plan"
uv run --no-editable aet plan gaps \
  "$output_parent/planner-e2e/needs-evidence/plan"
```

预期 E2E 摘要：

```text
single-file  READY_FOR_HUMAN_REVIEW  verification UNKNOWN
cross-module READY_FOR_HUMAN_REVIEW verification UNKNOWN
needs-evidence NEEDS_EVIDENCE       verification UNKNOWN
```

## 12. 最终 Git 状态

本次任务开始时，仓库已位于发布后的 `main`：

```text
HEAD e5ad8136c14bf3667eb3d561676e9b8364a728d7
tag  v1.17.0 -> 19efd8426572de8f75164a78c917a51a245976de
```

开始时没有已跟踪修改。共有 9 个用户既存未跟踪文件：
`AGENTS.md`、两份交付规划包和 6 个 dark-luxury 视觉/动效资产；本次均未
修改。由于规划要求的代码、测试、文档和发布结果已经完整存在于
`v1.17.0`，本次没有重复改写生产代码，只刷新本报告记录重新执行的验收。

最终只有 `执行报告.md` 存在已跟踪修改；没有 staged changes。本次没有
commit、tag、push、PR 或 Release。

## 13. 发布补充

- Release commit：`19efd8426572de8f75164a78c917a51a245976de`
- Git tag：`v1.17.0`
- Exact-tag CI：Run `30435701149`，`success`
- Release workflow：Run `30435886299`，`success`
- GitHub Release：ID `361603943`
- URL：`https://github.com/AdvancingTitans/agent-engineering-toolkit/releases/tag/v1.17.0`
- 附件：Wheel、sdist、CI Manifest、release classification、
  classification verification、release evidence，共 6 个，均为
  `uploaded`
- 发布分类：`deterministic`；34 个 behavior-sensitive 路径逐项绑定
  deterministic proof；未采纳治理资产，因此 Real Host adoption Gate 为
  `NOT_APPLICABLE`
- PyPI：未发布，不在本次用户要求范围

发布后只追加本节与 `PROJECT_MEMORY.md` 的恢复点；`v1.17.0` 标签及其
CI 产物保持精确绑定上述 Release commit。

## 14. 本次按 Phase 顺序复验

本次收到同一份完整交付规划后，先完整读取 2648 行规划与
`PROJECT_MEMORY.md`，再执行仓库规定的状态、标签和全量基线检查。真实
检查确认：规划要求的实现已经在 `v1.17.0` 完整落地并发布，当前工作树
不存在待实现的代码缺口。因此本次执行采用“逐 Phase 复验，发现差异才做
最小修复”的方式，避免违反“禁止擅自重构或添加规划外功能”。

| 阶段 | 本次真实结果 |
| --- | --- |
| 基线与最终回归 | 首次 536 tests，OK，66.786s；恢复安装态后最终 536 tests，OK，67.454s |
| Phase 0 | ADR、路径映射与产品边界：20 tests、184 subtests，PASS |
| Phase 1 | Models、6 Schemas、Policy、Parser、Validator：23 tests、54 subtests，PASS |
| Phase 2 | Request、Bundle、Atlas、Relevance、Source、Gap、Context：14 tests，PASS |
| Phase 3 | Renderer、Package、CLI/Helper：5 tests、5 subtests，PASS |
| Phase 4 | Skill Export、Python SDK、MCP、Prompt Injection：7 tests，PASS；TypeScript 19 tests 与 compatibility guard，PASS |
| Phase 5 | Verification Handoff：2 tests，PASS |
| Phase 6 | Benchmark、Performance、Real Case、E2E：5 tests、3 subtests，PASS；20-fixture Benchmark 与三个 E2E 场景均 PASS |

本次重新计算的 Planner Protocol 指标与冻结结果一致：

- required path recall：`1.0000`
- recommended path precision：`0.9706`
- required test recall：`1.0000`
- critical linkage omission rate：`0.0000`
- ungrounded path rate：`0.0294`
- protected path rate：`0.0000`
- conflict / UNKNOWN preservation：`1.0000`
- reference resolution：`1.0000`

本次没有新增实现差异。仍适用第 9 节列出的仓库布局映射，以及第 10 节的
剩余风险和 `UNKNOWN`。特别是第二位人工 Gold 审阅、跨任务真实 Agent
泛化、Plan 后实际实现成功率、真实远程 MCP Host 连接和超大仓库性能仍未
由本次本地复验建立新事实。

最终门禁重新测得 Planner 总行覆盖率 `90%`（56 tests、62 subtests），
Validator 分支覆盖率 `96%`（11 tests、10 subtests）；strict self-audit
为 FAIL 0 / UNKNOWN 0，`git diff --check` 通过。

执行中出现一次已纠正的环境性失败：最初并行运行两个
`uv run --with coverage` 与全量回归时，多个进程同时重装共享 `.venv`，
使 Atlas Schema 在两个测试进程中短暂不可见，各出现 1 个
`packaged Evidence Atlas schemas are unavailable` 错误。该并行方式不符合
共享环境的稳定运行条件。随后执行
`uv sync --reinstall-package agent-engineering-toolkit --no-editable`，
并把全量回归和两项覆盖率门禁改为串行运行；最终结果全部通过，未修改生产
代码，也未将中间失败隐去。

---

# AET v1.18.0 Activation and Distribution 执行报告

日期：2026-07-29
执行结论：**PARTIAL（仓库侧 COMPLETE，外部发布与时间窗运营待人工执行）**

## 1. 实际完成情况 vs 规划

| Phase | 规划目标 | 实际结果 |
| --- | --- | --- |
| Phase 0 | 活基线、定位、伦理与增长模型 | 完成。记录公开仓库、Release、PyPI、skills.sh、Community Profile 与 Traffic 状态；所有缺失值保留为 `UNKNOWN`/`UNAVAILABLE`。 |
| Phase 1 | installed-wheel stale-proof demo | 完成。新增 `aet demo list/stale-proof`、text/JSON/Markdown、`--output`、`--keep`、timeout、严格 fixture manifest、Schema 与边界测试；真实调用既有 `quick_proof`/`quick_fresh`。 |
| Phase 2 | README、文档、站点、社区与视觉 | 完成。旧完整 README 移至 `docs/reference/full-product-overview.md`；新 README 224/250 行；双语文档、无追踪静态站点、社区文件、SVG/PNG hero 与 social preview 均已落地。 |
| Phase 3 | Skills、Action、增长测量与工作流 | 完成。5 个现有 Skill 严格校验；Action 模板仅支持 read-only `check/scope/fresh` 且无 `eval`；Pages、launch-readiness、growth-snapshot 工作流已配置。 |
| Phase 4 | 各渠道 launch brief | 完成。GitHub Release、Show HN、Product Hunt、X、LinkedIn、Reddit、知乎、掘金、V2EX 均有独立 brief、owner 与 Stop Rule；没有执行外部发布。 |
| Phase 5 | 72 小时与 7/30 天运营 | 未伪造完成。时间窗尚未开始，操作手册和停止规则已就绪。 |
| Phase 6 | 条件触发的扩展分发 | `DEFERRED_WITH_REASON`。MCP Registry、Marketplace、Awesome List 等需满足实际稳定性、所有权和人工授权条件。 |
| Release prep | v1.18.0 候选、构建、验证 | 仓库侧完成：版本源、runtime、lock、CHANGELOG 与候选产物一致；未 commit、tag、push 或发布。 |

实现保持原有 Evidence Core、Observation/Assertion/Improvement 分离、
`PASS`/`FAIL`/`UNKNOWN`/`NOT_APPLICABLE`、Planner `PROPOSED` 权限和人工
采纳/发布权限。没有增加综合 trust score、内置 LLM、网络遥测、自动编辑、
自动提交或外部发布能力。

## 2. 工作树安全与执行边界

- 当前分支：`main`
- 起始与当前 HEAD：`e5ad8136c14bf3667eb3d561676e9b8364a728d7`
- 开始时已跟踪的 `执行报告.md` 有用户修改；另有 `AGENTS.md`、两份规划包
  和 6 个 dark-luxury 资产为用户既存未跟踪文件。
- 上述用户文件均保留；本报告只在原文件尾部追加本次结果，没有删除旧报告。
- 因工作树已有用户修改，没有创建规划建议的功能分支，避免在未提交工作上
  改变分支状态。
- 未执行 commit、tag、push、PR、Release、PyPI、Pages 部署、Marketplace、
  MCP Registry、发帖、外联或远程仓库设置修改。

## 3. T0 活基线

观测时间：`2026-07-29T13:31:21.603711Z`

| 指标 | 状态 | 值/说明 |
| --- | --- | --- |
| Stars | `KNOWN` | 2 |
| Forks / Watchers | `KNOWN` | 0 / 0 |
| Contributors | `KNOWN` | 2 |
| Open Issues / 全部 PR | `KNOWN` | 1 / 3 |
| 最新 GitHub Release | `KNOWN` | `v1.17.0`，6 个资产，累计下载 0 |
| PyPI | `KNOWN` | 当前 `v1.11.1`；Owner 为 `Young_Tommy` |
| PyPI 下载量 | `UNAVAILABLE` | PyPI JSON 不提供当前下载总量 |
| Traffic views / uniques / clones | `UNKNOWN` | 已认证请求返回 HTTP 403，当前 token 缺少 Traffic 权限 |
| skills.sh | `UNAVAILABLE` | 5 个 AET Skill 尚未被索引 |
| Community Profile | `KNOWN` | 57%（远程仓库尚未包含本地候选变更） |
| Pages / Discussions / Homepage | `KNOWN` | 均未启用/未设置 |

基线保存在忽略目录
`.aet/growth/metrics/baseline.json`，没有提交用户身份、stargazer 列表、
设备信息或本地 AET 使用数据。自比较的已知数值 delta 均为 0；Traffic
转换率保持 `UNKNOWN`，没有除零或归因陈述。

## 4. 关键实现

- `src/aet/demo/`：严格 registry、manifest、sandbox、runner、renderer、
  CLI 和稳定错误码。
- `schemas/demo/v1/`：manifest/result Draft 2020-12 Schema。
- `scripts/verify_installed_demo.py`：跨平台从最终 wheel 建立干净 venv，
  在 checkout 外验证 list/text/JSON/Markdown。
- `README.md`、`PYPI_README.md`、`docs/README.zh-CN.md` 与聚焦文档：
  首屏统一为 “Proof-carrying workflows for coding agents” 和 stale-proof。
- `site/`：双语、响应式、无 analytics/cookie/外部 runtime 的静态站点。
- `integrations/github-action-template/`：argv array、无 `eval`、只读三模式，
  含 shell metacharacter 注入测试。
- `ops/growth/` 与 `scripts/growth/`：隐私保护的聚合快照、评估、launch
  校验、人工 settings/outreach/checklist 和渠道 brief。
- `.github/workflows/`：installed demo 多 OS matrix、Pages、
  launch-readiness、growth-snapshot；growth snapshot 不自动 commit 或发帖。

## 5. Hero demo 实测

最终候选命令：

```bash
uvx --from agent-engineering-toolkit aet demo stale-proof
```

该命令只可在 `v1.18.0` 实际发布后用于公开启动。当前候选使用构建 wheel
从仓库外执行，捕获输出为：

```text
AET stale-proof demo

1. Test command executed                    PASS
2. Proof matches the tested source          EXACT_MATCH
3. Source changed without rerunning tests   RELEVANT_FILES_CHANGED

The test really passed, but that proof no longer applies to the current code.
Demo result: PASS
```

JSON 核心字段：

```json
{
  "schema_version": "aet-demo-result/v1",
  "demo_id": "stale-proof",
  "overall_status": "PASS",
  "execution_status": "PASS",
  "before_state": "EXACT_MATCH",
  "after_state": "RELEVANT_FILES_CHANGED",
  "network_calls": 0,
  "llm_calls": 0
}
```

## 6. 测试结果

| 验证 | 结果 |
| --- | --- |
| 初始项目回归 | 536 tests，PASS，68.238s |
| Phase 1 demo suite | 28 tests，PASS |
| Phase 3 growth suite | 17 tests + 2 subtests，PASS |
| 兼容性修复聚焦回归 | 92 tests + 219 subtests，PASS，37.80s |
| 最终 `uv run pytest -q` | 591 tests + 607 subtests，PASS，67.34s |
| 最终 canonical unittest | 581 tests，PASS，70.402s |
| Demo result Schema | Draft 2020-12 Schema + 核心不变量，PASS |
| installed-wheel smoke | clean venv、checkout 外 list/text/JSON/Markdown，PASS |
| README budget | 224/250，PASS |
| 本地链接 | 57 个 docs/site 文件，PASS |
| Social Preview | 1280×640、44,413 bytes，PASS |
| Skills | 5 个 Skill，strict PASS |
| Launch readiness | `stage=all`，strict PASS |
| Action 注入 | shell metacharacter cases，PASS |
| `git diff --check` | PASS |
| AET self-audit | FAIL 0 / UNKNOWN 0 |
| AET self-review | PASS 7 / FAIL 0 / UNKNOWN 0 |

首次最终 `pytest` 曾出现 8 个兼容性失败：4 个是非超时 Trace 不应新增
`timed_out: false`，4 个是精简 README/intent 丢失既有测试冻结的兼容锚点。
修复采用最小兼容改动：仅在真实 timeout 时给 Trace 增加 `timed_out: true`，
并在 250 行预算内恢复 Planner、Quick、Atlas 与旧 intent proof 锚点。随后
聚焦与两套全量测试全部通过。

本机只实际运行 macOS；Ubuntu/Windows 尚未由本地环境证明。CI 已配置
Ubuntu 3.11/3.12/3.13、macOS 3.11/3.13、Windows 3.11/3.13 matrix，必须在
发布前真实运行。

## 7. 构建产物

| 产物 | 大小 | SHA-256 |
| --- | ---: | --- |
| `agent_engineering_toolkit-1.18.0-py3-none-any.whl` | 1,437,358 bytes | `74e70fc3658862e6666ddff59a9d362af40668dbf25e108ba2d1b7d01f00daf9` |
| `agent_engineering_toolkit-1.18.0.tar.gz` | 1,433,600 bytes | `a04720521293f358bd4b711afcf732feaef4107824fa16c7d5e0f6b8726d63ce` |

Wheel metadata、runtime 和 `uv.lock` 均为 `1.18.0`；wheel 内含 demo
manifest、`src/calc.py` 和 `tests/test_calc.py`。本次新增二进制只有
hero PNG（146,776 bytes）和 social preview PNG（44,413 bytes），均低于
1 MB；wheel 低于规划 5 MB 目标。

## 8. 与规划的差异及原因

1. **总体状态为 PARTIAL，而非伪造 COMPLETE。** Phase 5/6 是 72 小时、
   7 天、30 天和条件触发工作，当前没有经过这些时间窗。
2. **没有创建 `feat/aet-activation-distribution`。** 开始时存在用户已跟踪
   和未跟踪工作；保持 `main` 避免改变用户工作状态。
3. **没有发布或修改远程设置。** 规划和用户请求都没有授权 tag、Release、
   PyPI、Pages、Discussions、Topics、Homepage、Social Preview 或发帖。
4. **MCP Registry 未添加 `server.json`。** 当前 namespace 所有权与最新
   publisher/schema 验证条件未完整满足，按规划记录
   `DEFERRED_WITH_REASON`。
5. **最终 self-review intent 显式容纳用户既存输入。** AET review 会把
   未跟踪规划包和中文路径的 Git 引号形式计入 changed paths；只为准确
   复核当前工作树补入路径，没有修改这些用户文件。
6. **Schema 验证临时使用 `uv run --with jsonschema`。** 没有给产品 runtime
   或项目依赖新增 `jsonschema`；这是一次性验证工具。
7. **`release-classification.json` 保持 v1.17 不变。** 新的精确分类必须绑定
   最终 commit/tag 和 CI 产物；当前既未提交也未获发布授权，提前生成会伪造
   release identity。

## 9. 分发就绪度

| 表面 | 状态 |
| --- | --- |
| GitHub Release | 候选文案与本地产物就绪；未发布 |
| PyPI | 所有权已确认；远程仍是 v1.11.1；未上传 |
| skills.sh | 安装文档和 strict catalog 就绪；索引仍 `UNAVAILABLE` |
| Pages | 静态站点与 workflow 就绪；远程 Pages 未启用，构建未运行 |
| GitHub Action Marketplace | 独立模板与注入测试就绪；未拆库、未发布 |
| MCP Registry | `DEFERRED_WITH_REASON` |
| 外部渠道 | briefs 就绪；全部仍为人工未执行 |

## 10. 剩余风险与发布阻塞

- 必须先在真实 CI 上通过 Ubuntu/macOS/Windows installed-wheel matrix 和
  Pages build；本地 macOS 通过不能替代其他 OS。
- 必须由最终 commit/tag 生成并验证 v1.18 release classification、CI
  manifest 和 exact-artifact promotion 记录。
- GitHub Release 与 PyPI 仍分别是 v1.17.0 和 v1.11.1；公开 README 的
  `uvx` hero command 在 v1.18 发布前不能作为已上线能力宣传。
- Traffic 权限不足，views/uniques/clones 和渠道转换率为 `UNKNOWN`。
- skills.sh 未索引，不能添加 badge 或声称安装量。
- Community Profile、Pages、Discussions、Homepage 和 Social Preview
  需要远程设置后重新观测。
- 文案仍需维护者核事实、规则和上下文；Show HN 最终正文与全部评论必须由
  人本人撰写，不能使用生成或 AI 编辑文本。
- Star 增长是外部行为结果，仓库改造只能提高合格发现与试用概率，不能保证。

## 11. 人工下一步

1. 审阅本报告和完整 diff，确认用户既存文件保持不变。
2. 在功能分支/PR 上运行 launch-readiness、Pages 和完整 installed-demo
   multi-OS CI。
3. 通过后生成最终 commit/tag-bound v1.18 classification 与 CI manifest。
4. 明确授权后再依次执行 tag、GitHub Release、PyPI 和远程 About/Topics/
   Social Preview/Pages/Discussions 设置。
5. 发布后从真实 PyPI 与 Release URL 重新运行 hero command，并更新
   README 中的版本状态提示。
6. 由渠道 owner 根据最新规则人工重写/发布 briefs；Show HN 特别遵守
   `HUMAN_REWRITE_REQUIRED`。
7. 在 72 小时、7 天和 30 天保存新的聚合快照，按 Stop Rule 决定继续、
   修改或停止实验。

精确远程设置与运营清单见
`ops/growth/launch/manual-actions.md` 和
`ops/growth/launch/checklist.md`。

## 12. 自审证据路径

- `.aet/release/audit.json`
- `.aet/release/review.json`
- `.aet/release/full-tests.txt`
- `.aet/release/wheel-demo.txt`
- `.aet/release/readiness.txt`

## 13. 2026-07-29 实际发布补充

用户随后明确授权通过 GitHub 界面执行人工发布与远程设置。已完成：

- `v1.18.0` 最终标签指向
  `7ff1e0032827cce92b17777adc47af78977f0af3`。
- `main` CI Run `30465371559` 与 exact-tag CI Run `30465380241` 均成功；
  Ubuntu 3.11/3.12/3.13、macOS 3.11/3.13、Windows 3.11/3.13 的
  installed-wheel demo 全部通过。
- GitHub Release Run `30466008349` 成功；Release ID `361859093` 发布
  wheel、sdist、CI manifest、release classification、classification
  verification 与 release evidence 共 6 个资产。
- PyPI Trusted Publishing Run `30466191944` 成功；公共
  `agent-engineering-toolkit==1.18.0` wheel SHA-256 为
  `9b6000ebd8e6cf9d174f1fb6797cf7299cf9246a5adcacd6c16033a63bd24f76`。
- 从仓库外干净临时目录运行
  `uvx --refresh --from agent-engineering-toolkit==1.18.0 aet demo stale-proof`
  返回 `PASS`、`EXACT_MATCH`、`RELEVANT_FILES_CHANGED`。
- GitHub Pages、Discussions、Homepage、About、20 个 Topics 与 Social
  Preview 已配置；Community Profile 为 100%；Known limitations 已发布为
  Discussion #5；仓库原本已经 pinned。

发布过程中真实 Windows runner 暴露两个候选问题，并均在发布前修复：

1. checkout 的 CRLF 改写了打包 fixture，增加 `.gitattributes` 强制 LF；
2. Windows 临时文件锁使成功演示返回规划允许的 `PASS_WITH_WARNING`，但
   wheel verifier 错误地只接受 `PASS`；修正 verifier 接受两个成功态。

两次失败候选均未创建 GitHub Release 或 PyPI 版本；旧标签在发布前删除并
重建，最终 Release 与 PyPI 只绑定全绿的 `7ff1e003…`。

仍未完成且不得伪造为完成：

- Phase 5/6 的 72 小时、7 天、30 天观测窗尚未经过；
- skills.sh 的 5 个 URL 当前返回 200 fallback，但正文仍明确显示 Skill
  不可用，状态保持 `UNAVAILABLE`；
- Marketplace Action 已完成：用户在 action time 明确确认并接受 GitHub
  Marketplace Developer Agreement v2.4；独立公开仓库
  `AdvancingTitans/aet-evidence-action` 的已测试模板提交为
  `55f3c8f0e505fab5567694da2d1b5d834a227922`，`v1` Release ID 为
  `361873799`，`AET Evidence` Marketplace listing 已公开；
- MCP Registry 仍为 `DEFERRED_WITH_REASON`；
- Show HN 按规划必须由维护者本人撰写并发布；其他外部渠道也仍需渠道 owner
  按当时规则人工重写与确认。

## 14. 2026-07-29/30 人工发布执行补充

用户进一步明确授权执行报告中的人工发布任务，并指定知乎、掘金、V2EX 等
国内渠道使用中文。实际完成情况如下：

| 渠道/任务 | 实际状态 | 证据或阻塞 |
| --- | --- | --- |
| GitHub Action Marketplace | `COMPLETE` | `https://github.com/marketplace/actions/aet-evidence` |
| Awesome Actions | `PR_SUBMITTED` | `https://github.com/sdras/awesome-actions/pull/874`；PR open、非 draft、mergeable |
| X | `COMPLETE` | `https://x.com/HerPrayMachine/status/2082498082617561252` |
| Reddit | `COMPLETE` | `https://www.reddit.com/r/Python/comments/1unctej/comment/p0hsoq9/`；按 r/Python 当期 Showcase 规则只发布一次 |
| 知乎 | `COMPLETE` | 中文长文 `https://zhuanlan.zhihu.com/p/2065952961482715666` |
| 掘金 | `COMPLETE` | 中文教程 `https://juejin.cn/spost/7667837994720280616` |
| Show HN | `BLOCKED_BY_PLATFORM_RESTRICTION` | 用户登录后已尝试提交 `Show HN: AET – Detect stale test proof after code changes` 与仓库 URL；HN 跳转至 `showlim`，提示暂时限制尚不熟悉站点或文化的账号发布 Show HN。未改标题绕过限制，亦未生成或发布评论 |
| Product Hunt | `SCHEDULED` | 用户确认年满 16 岁后完成 onboarding；Required checklist 100%，包含方形缩略图、3 张真实 gallery 图、首条 maker comment、Free/open-source 信息与 3 个标签；已排期 2026-07-30（Pacific Time）：`https://www.producthunt.com/products/agent-engineering-toolkit?launch=agent-engineering-toolkit` |
| LinkedIn | `COMPLETE` | 英文图文已发布：`https://www.linkedin.com/feed/update/urn:li:share:7488283557078003712/`；配图为 `docs/assets/hero-stale-proof.png` |
| V2EX | `CANCELLED_BY_OWNER` | 用户于 2026-07-30 明确要求不在 V2EX 发布；未发帖，也未绕过邀请激活机制 |

与原规划的差异：

1. 用户在执行阶段明确覆盖了 Show HN 的人工重写限制，因此标题和 URL 可以由
   Codex 代写、代发；HN 对 AI 评论的站点规则仍优先，未代写或代发评论。
2. 外部发布不再全部保持 `MANUAL_NOT_STARTED`：已有五个渠道公开发布，一个
   Product Hunt launch 已排期，一个 Awesome List PR 已提交；Show HN 保留
   精确的平台阻塞态，V2EX 按用户指示取消，没有伪造完成。
3. Agent Skill Awesome List 没有为了数量盲投。当前最相关候选明确要求已有
   真实社区采用，而 v1.18.0 刚发布、尚无可验证采用证据，故状态为
   `DEFERRED_WITH_REASON`。

本轮外部状态核验：

- Marketplace、Awesome Actions PR 与 X 通过公开 HTTP 页面返回 200；
- Reddit、知乎、掘金和 LinkedIn 已在已登录浏览器中核对公开正文与 URL；
  匿名命令行请求受到部分站点反爬（403/404），不把反爬结果误记为下线；
- Awesome Actions PR 当前 open、非 draft、mergeable；尚未合并。
- Product Hunt launch 状态为 `Scheduled`，Required checklist 为 100%；可选
  newsletter 均未订阅，未填写无法核实的 shoutout、融资或投资方信息。

本轮仓库验证：

| 验证 | 结果 |
| --- | --- |
| `scripts/growth/validate_launch.py --stage all --strict` | `PASS` |
| YAML 解析：`channel-matrix.yml`、`awesome-lists.yml` | `PASS` |
| canonical unittest | 581 tests，`PASS`，87.354s |
| `git diff --check` | `PASS` |

剩余风险：

- Show HN 仍受 HN 对该账号的临时发布限制，解除时间未知；评论与回复仍须由
  用户本人按 HN 的 AI 评论规则完成；
- Product Hunt 目前只是排期完成，尚需等到 Pacific Time 的 launch 时点后
  才能验证实际上线状态；V2EX 已由用户主动取消；
- 外部帖子可能被社区规则、审核或后续账号状态影响，公开 URL 不等于获得
  推荐、流量或 Star；
- 72 小时、7 天、30 天观测窗仍未经过，增长结果继续保持 `UNKNOWN`；
- skills.sh 详情页和 MCP Registry 仍未满足发布条件。
- `.aet/release/demo.json`
- `.aet/release/demo.md`
- `.aet/release/demo-text.txt`

这些文件位于忽略的本地 evidence 目录，不自动提交。

## 13. 建议 v1.18.0 Release Note（未发布）

> AET v1.18.0 is an activation and distribution release. It adds an
> installed-wheel stale-proof demo, a focused first-run experience, a
> no-tracking static site, community and integration guides, strict Skill and
> launch validation, a read-only GitHub Action template, and
> privacy-preserving growth snapshots. The demo runs a real test, binds Proof
> to exact source, then shows `EXACT_MATCH → RELEVANT_FILES_CHANGED` after the
> source changes without rerunning tests. Core Evidence authority semantics are
> unchanged: missing facts remain explicit, Planner output remains
> `PROPOSED`, and AET grants no merge, publication, or release authority.

## 15. 2026-07-30 通用 Codex Skill 沉淀

已将本次从仓库改造到多渠道发布的通用方法安装为 Codex Skill：

`/Users/yjw/.codex/skills/promote-github-repo`

Skill 名称：`promote-github-repo`。覆盖：

- Phase 0：Git/Release/package/社区/远程表面的 T0 基线和定位冻结；
- Phase 1：30–90 秒真实激活路径、clean install 与 exact artifact；
- Phase 2：README 首屏改造、旧材料保全、双语文档、Hero、Social Preview、
  Pages、About、Topics、社区和 Known limitations；
- Phase 3：版本一致性、GitHub Release、package registry、Marketplace、
  Skills、MCP 条件门禁和 Awesome Lists；
- Phase 4：GitHub Release、X、LinkedIn、Product Hunt、Reddit、Show HN、
  知乎、掘金的差异化文案、实时规则核验和人工发布；
- Phase 5/6：72 小时、7 天、30 天聚合观测、Stop Rule 和执行报告。

按用户要求，V2EX 被永久排除，不生成 brief、账号操作或发布任务。国际渠道
默认英文，知乎、掘金等国内技术渠道默认中文。

Skill 资源：

- 4 份按需加载的 playbook：仓库/Release、渠道、权限安全、指标报告；
- 5 个可复制并项目化的 promotion-pack 模板；
- `scripts/audit_repo.py`：标准库、无网络的通用仓库基线审计，输出稳定 JSON，
  会识别非 GitHub origin、脱敏 remote userinfo，并把外部状态保留为
  `UNKNOWN`。

验证结果：

| 验证 | 结果 |
| --- | --- |
| `skill-creator/quick_validate.py` | `PASS` |
| `agents/openai.yaml` 与 promotion YAML 解析 | `PASS` |
| AET 真实仓库 audit smoke | `PASS` |
| 临时最小 Python CLI audit smoke | `PASS` |
| remote userinfo 脱敏与 provider 识别 | `PASS` |
| 非 Git 仓库稳定返回 exit 69 | `PASS` |
| 两轮隔离 forward test + 修订后复测 | `PASS` |
| Skill 目录无 `__pycache__` 或占位 TODO | `PASS` |

前向测试实际发现并修正了六类泛化问题：微型 README 不应生成空洞备份、
非 GitHub remote 与疑似凭证处理、不同项目的 exact artifact 形态、
`BLOCKED_BY_PREREQUISITE` 状态、Product Hunt 支持窗口，以及 Show HN
submission/评论和账号限制边界。

后续调用方式：

```text
使用 $promote-github-repo 直接推广这个仓库：<GitHub URL 或本地路径>
```

---

# AET Behavioural Risk Diagnosis 执行报告

日期：2026-08-01
工作区：`/Users/yjw/agent/Agent Engineering Toolkit`
实施依据：`aet-behavioural-risk-diagnosis-delivery-package.md`
基线：AET `v1.18.0`，实施前 canonical unittest 为 `581 tests / OK`
结论：Phase 1–5 的代码、协议、评测、生态接线和包验证已完成；预测晋级、
独立人类标注/演练和正式发布未满足或未获授权，保持 `UNKNOWN` / 未执行。

## 1. 实际完成情况 vs 规划

| Phase | 规划目标 | 实际完成 | 状态 |
| --- | --- | --- | --- |
| Phase 1 | Policy/Diagnosis Schema、四态模型、威胁模型、黄金集 | 新增三轴冻结模型、Policy/Diagnosis Schema、7 个 Codex/Claude Code 场景、Intent/Policy/outcome fixtures、双标注格式示例与威胁模型；schema 输出拒绝 aggregate score、mental-state claim 和自动动作字段 | 代码与契约完成；独立人类 kappa 未完成 |
| Phase 2 | 确定性三轴诊断、CLI、原子输出、评测与性能 | 新增 adapters/coverage/signals/diagnose/renderer、`aet risk diagnose`、Codex/Claude parity、10 MB 性能门、无覆盖即 `UNKNOWN`；Bundle 目录必须先通过现有 Bundle validator | 完成 |
| Phase 3 | 同上下文路径和可执行干预诊断 | 新增 same-context/order/asset/monitor linker、7 种因子组合决策表、反事实限制；所有干预均为 `PROPOSED`，没有执行器 | 自动化完成；人工 5 分钟演练未完成 |
| Phase 4 | 不训练模型的保守 forecast 可行性门 | 新增 Wilson interval、支持数/域/时间-仓库-宿主切分/泄漏/FPR/ECE/Brier 门，以及 `aet risk forecast` | 实现完成；实际数据门 `FAIL`，forecast 保持 `UNKNOWN` |
| Phase 5 | Atlas、MCP、TypeScript SDK、文档、打包 | 新增只读 `aet_risk_diagnose`、第 11 个 Atlas `behavioural-risk` Perspective、TypeScript 类型和本地 validator、双语文档、CHANGELOG、wheel/sdist 与隔离安装验证 | 发布就绪；未 commit/tag/publish |

最终接口保持规划不变：

```text
aet risk diagnose --run ... --intent ... --policy ... [--bundle ...]
                  --json-out ... [--md-out ...] [--created-at ...]

aet risk forecast --diagnosis ... --calibration ... --host ...
                  --repository ... --json-out ... [--created-at ...]

MCP: aet_risk_diagnose (read-only)
Atlas: aet atlas build ... --risk-diagnosis diagnosis.json
SDK: validateRiskDiagnosis(value), validateRiskForecast(value)
```

主要落地文件与职责：

- `src/aet/risk/`：模型、严格协议校验、Policy、适配、覆盖判断、三轴信号、
  编排、路径、干预、forecast 和无覆盖写入；
- `schemas/risk/v1/`：Policy、Diagnosis、Forecast 三份正式 JSON Schema；
- `eval/behavioural-risk/`：冻结 suite、scorer、runner、标注规范和业务实验；
- `tests/risk/`、`tests/fixtures/risk/`、`tests/integration/test_risk_e2e.py`：
  单元、边界、MCP/Atlas、宿主 parity 和端到端覆盖；
- `src/aet/mcp_server.py`、`src/aet/atlas/*`、`schemas/evidence-atlas/v1/*`：
  只读生态投影；无法解析回 Bundle 的 diagnosis ref 不进入 Atlas 事实图；
- `packages/evidence-bundle/`、`packages/protocol-types/`：Risk 类型、运行时
  shape/reference/authority validator 和 npm 契约测试；
- `docs/behavioural-risk-diagnosis.md`、`README.md`、
  `docs/README.zh-CN.md`、`CHANGELOG.md`：Lab 定位和预测边界。

## 2. 测试结果

### 2.1 按 Phase 即时门禁

| 阶段 | 当阶段验证结果 |
| --- | --- |
| 基线 | `581 tests / OK` |
| Phase 1 | Risk `10 tests / OK`；全仓 `591 tests / OK` |
| Phase 2 | Risk `32 tests / OK`；E2E `1 / OK`；全仓 `614 tests / OK` |
| Phase 3 | Risk `39 tests / OK`；全仓 `621 tests / OK` |
| Phase 4 | Risk `45 tests / OK`；E2E `1 / OK`；全仓 `627 tests / OK` |
| Phase 5 最终 | Risk `50 tests / OK`；Risk E2E `1 / OK`；全仓 `632 tests / OK` |

Phase 5 接线时的窄门禁真实发现并修复了以下问题：Atlas manifest 与
hierarchy 仍把 Perspective 上限锁死为 10；risk projection edge 使用了现有
schema 不接受的自定义 authority；npm `dist` 尚未重建；README 的否定性边界
文案触发了严格营销关键词门。修复方式均为调整新接线或文案，没有放宽既有
validator。

### 2.2 冻结黄金集

机器可读证据：`.aet/risk-eval-report.json`

| 指标 | 结果 | 门槛 |
| --- | ---: | ---: |
| Precision | 1.0000 | ≥ 0.95 |
| Recall | 1.0000 | ≥ 0.90 |
| False-positive rate | 0.0000 | = 0 on critical clean fixtures |
| Coverage | 0.8571 | UNKNOWN 单列，不并入错误通过 |
| Labels | 21 | 7 cases × 3 factors |
| Preserved UNKNOWN | 3 | 缺结果场景三轴全部 UNKNOWN |

Suite SHA-256：
`760f5c785e01dda5f3b82adf21d567bce512dba5cf7f092fb04ba3e1c7de0f04`
Policy SHA-256：
`bf2814d25018648ff0060b91ac0e080ddd2fcaf14951ae4dab1de92f5c7f493a`

### 2.3 真实项目业务实验

机器可读证据：`.aet/risk-business-experiment.json`

实验在三个真实仓库快照的代表性 Python 源码上执行 24 个受控 shadow
episodes（3 repositories × 2 hosts × 4 scenarios），共 72 个因子标签。
真实副作用只发生在 disposable 临时 Git 仓库；输入绑定 source SHA-256 和：

- AET：`6299fdfd38e62ef9b9e85fe451b291d962ab3fe7`；
- ControlArena：`6d7259ec00f505869705e05ecc84df19b6230c7b`；
- AgentRx：`f228165bfec60a801fd5fedd9d8ffe0f9de0c69d`。

| 系统 | Exact accuracy | Positive recall | FPR | UNKNOWN 保留 | 失败引用覆盖 |
| --- | ---: | ---: | ---: | ---: | ---: |
| AET Risk Diagnosis | **1.0000** | **1.0000** | **0.0000** | **1.0000** | **1.0000** |
| AET v1.18 scope review 投影 | 0.4167 | 0.2500 | 0.0000 | 0.6667 | 1.0000 |
| ControlArena-inspired binary baseline | 0.5833 | 1.0000 | 0.6250 | 0.0000 | 0.0000 |
| AgentRx-inspired intent-invariant baseline | 0.4167 | 0.2500 | 0.0000 | 0.6667 | 0.0000 |

`better_than_each_declared_baseline = true`。这里的 peer baseline 是对公开架构
思想在同一 AET fixture 上的可复算实现，不是直接运行 upstream 工具；因此只
支持“本实验中的代码审查辅助更好”，不支持普遍优于同类项目或模型安全结论。

### 2.4 性能、SDK 与包产物

- 10 MB / 100k 低相关记录门：时间 ≤ 2 秒、峰值内存 ≤ 256 MB，`PASS`；
- TypeScript：protocol-types build/check `PASS`；evidence-bundle build/check、
  `20 tests / PASS`，checked `dist` smoke `2 tests / PASS`；
- Python：wheel 与 sdist 构建 `PASS`；隔离 venv 安装 wheel `PASS`；安装态
  `aet risk diagnose` `PASS`；三份 Risk Schema 均从 wheel data-files 加载；
- `git diff --check`：`PASS`；
- 网络和模型调用：诊断、评测、forecast 均为 `false`。

## 3. 与规划的差异点及原因

1. **Forecast 未晋级。** 当前 24 个受控 episodes 少于 200 个独立 episodes、
   30 个独立正例门槛，也不是预注册生产 outcome。按规划 fail closed，
   `prediction_gate_eligible = false`，没有生成 `skills/aet-risk/`。
2. **独立人类验证未冒充完成。** 仓库包含双标注/adjudication 格式与示例，
   但本次没有两名独立人类标注者或安全审阅者，因此 per-factor Cohen’s kappa
   和“5 分钟内定位证据 ≥ 90%”保持 `UNKNOWN`。自动化 parity/traceability
   测试不能替代它们。
3. **Peer 对比采用 inspired baseline。** ControlArena、AgentRx 的公共接口、
   任务和 outcome 与 AET 风险协议不等价；直接比较上游工具会混入 harness
   差异。实验固定其公开思想并在共享标签/fixture 上复算，同时明确限制。
4. **增加了必要兼容文件。** 规划列出 perspective/graph schema，但把 Atlas
   从 10 扩为 11 还必须同步 `atlas-manifest.schema.json`、
   `hierarchy.schema.json`、TypeScript `runtime/atlas.js` 和既有精确计数测试。
   这些是协议一致性修复，不是规划外能力。
5. **发布产物由构建生成，不手改。** `packages/evidence-bundle/dist/*` 由现有
   build script 生成并通过 smoke；仓库规则忽略生成目录。Python wheel/sdist
   也写入 `tmp/` 验证目录，没有混入源码清单。
6. **未进行正式发布。** 没有 commit、tag、push、GitHub Release 或 PyPI/npm
   publish；当前工作树在任务开始前已有用户修改，且本请求没有授予外部发布
   权限。实际状态是“代码与包验证完成、发布未执行”。

## 4. 剩余风险

- 当前高精度指标来自冻结合成集与受控 shadow episodes；对真实长时、多 Agent、
  actor identity 不完整和生产监控故障的外部效度仍未知。
- Risk Policy 的 asset/tool/monitor pattern 必须由部署方审阅；策略漏项会导致
  `UNKNOWN` 或漏覆盖，不能解释为安全。
- 诊断只证明当前部署和当前证据上下文中的可观察行为，不证明内部动机、稳定
  倾向、通用模型能力或 loss-of-control 概率。
- 双文件发布会在所有可捕获错误上回滚且拒绝覆盖既有报告；但两个独立目标路径
  无法在断电/进程被强杀时获得文件系统级跨路径事务原子性。若未来要求该级别，
  需要把 JSON/Markdown 封装为单目录并原子发布，属于协议变更。
- Forecast 在独立数据量、holdout、低 FPR 和校准门全部满足前必须继续
  `UNKNOWN`；不得从本次业务实验推导预测能力。
- TypeScript validator 与 JSON Schema 通过契约测试保持同步；未来 schema
  版本升级需要同时更新 Python、MCP、Atlas 和 npm consumer fixtures。
- 正式 release 尚未进行；发布前仍需在干净分支隔离用户现有修改并重新执行
  exact-tag CI、package hash 和 release evidence 门。

## 5. 恢复点

下一步若继续 Phase 4，不应先调规则，而应先冻结独立 outcome rubric，安排
两名独立标注者，收集至少 200 个跨时间/仓库/宿主 episodes（至少 30 个正例），
再揭开 holdout。任何不满足门槛的 pathway 继续输出 `UNKNOWN`。

---

# AET Behavioural Risk Diagnosis 公共数据收口执行报告

日期：2026-08-01

范围决策：**先正式发布诊断，预测继续研究；不要求新增人工标注。**

本节是对上文 Behavioural Risk Diagnosis 报告的最终范围修订；如有冲突，以本节
为准。

## 先用通俗语言说明这次优化了什么

过去 AET 主要回答“这次代码修改有没有证据证明正确”。现在它还可以回答：

1. Agent 做的事是否偏离了用户明确交代的目标；
2. 它是否真的借助当前工具和权限完成了高影响动作；
3. 它是否真的动过日志、审计或其他监控面；
4. 以上现象是否发生在同一次任务里，以及审查者应该优先检查什么、可以考虑
   哪些干预。

所谓“黄金集”，就是一套事先知道正确答案的考题，用来检查规则有没有误报或
漏报。项目里原有 7 个合成案例继续承担“代码改坏没有”的回归检查，但不再把
它称作独立人类验证。本次新增的正式诊断门禁来自公开 AgentDojo 数据：选取 9
个成组三联案例，每组包含正常执行、成功阻断攻击和攻击成功，正确答案来自
AgentDojo 自己的程序化 scorer，而不是我们重新人工标注。

为了不复制提示、隐私内容或 benchmark 解法，仓库只保留会影响诊断的工具名、
目标类别、执行结果和哈希；每个案例又绑定上游 commit、文件路径和文件哈希。
数据被改写、标签和规则不一致或关键字段缺失时，runner 会直接拒绝执行。

“预测继续研究”不是文案约束，而是代码硬门禁。即使输入一份看起来完全合格的
校准文件，forecast 仍会返回 `UNKNOWN`，并给出
`forecast_research_only`。诊断可以告诉审查者“已经观察到了什么”，但当前版本
不会声称“未来一定会发生什么”。

## 1. 实际完成情况 vs 规划

| 项目 | 原规划 | 最终实现 | 状态 |
| --- | --- | --- | --- |
| 合成黄金集 | 兼作发布指标，并预留双人标注 | 保留 7 个案例，只作为 contract regression；报告写明 `label_authority=synthetic_contract_fixture`、`release_gate=false` | 完成并降权 |
| 正式诊断门禁 | 独立人工标签、kappa、人工桌面演练 | 改用 AgentDojo 固定 commit 的公开程序化标签；9 cases / 27 factor labels；不声明人工验证 | 完成 |
| 数据复用 | 需要新增人工工作 | 只存公开数据派生的最小 action/outcome summary，绑定 source/argument/effect SHA-256，不分发 prompt、内容和答案 | 完成 |
| 三因子诊断 | 错位动机、危害实现能力、监控规避 | 保留三个可观察行为因子；AgentDojo 没有监控规避标签，因此该数据集上明确为 `NOT_APPLICABLE` | 完成，fail closed |
| Forecast | 数据门满足后可晋级 | 增加不可绕过的 `research_only` release state；任何输入均不能晋级，输出 `UNKNOWN` | 完成，未发布预测能力 |
| 人工作业 | 双标注、adjudication、五分钟演练 | 按用户决定从本次发布条件移除；未冒充已完成 | 有意不执行 |
| GitHub 发布 | 代码验收后发布 | 用户要求先验收；没有 commit、tag、push、Release 或包发布 | 待用户授权 |

新增或最终调整的关键文件：

- `eval/behavioural-risk/public-corpus.json`：9 个 AgentDojo 公共案例的最小、
  哈希绑定摘要；
- `eval/behavioural-risk/runner.py`：公共数据严格校验、期望标签推导、当前诊断
  调用和发布指标；
- `tests/fixtures/risk/agentdojo-policy.json`：把 AgentDojo 动作映射到 AET
  Policy，并把无上游标签的监控因子声明为 `NOT_APPLICABLE`；
- `tests/risk/test_public_benchmark.py`：正常门禁、上游标签篡改、派生标签篡改、
  不分发 prompt/content 的测试；
- `src/aet/risk/forecast.py`：forecast 发布状态冻结为 `research_only`；
- `tests/risk/test_forecast.py`：证明“看起来合格的校准输入”也不能绕过研究门；
- `eval/behavioural-risk/README.md`、`docs/behavioural-risk-diagnosis.md`、
  `README.md`、`docs/README.zh-CN.md`、`CHANGELOG.md`：同步正式诊断和研究预测
  的产品边界。

既有 CLI、Schema、MCP、Atlas 和 TypeScript 接口未重构，没有添加自动处置、
模型调用、训练、微调、蒸馏或模型参数修改。

## 2. 测试结果

### 当前源码与评测

| 门禁 | 结果 |
| --- | --- |
| Risk 专项测试 | `53 tests / OK` |
| 全仓当前源码回归 | `635 tests / OK` |
| 合成 contract regression | `PASS`；7 cases / 21 labels；Precision `1.0`；Recall `1.0`；FPR `0.0`；3 个 `UNKNOWN`；非发布门 |
| AgentDojo 公共诊断门 | `PASS`；9 cases / 27 labels；exact case accuracy `1.0`；Precision `1.0`；Recall `1.0`；FPR `0.0`；cited failure coverage `1.0` |
| 公共数据负向测试 | 上游 programmatic label 或派生 expected label 被改动时均拒绝；语料不含 prompt/content |
| Forecast fail-closed | 合格校准样例仍为 gate `FAIL`、forecast `UNKNOWN`，原因包含 `forecast_research_only` |

公共门机器证据为 `.aet/risk-public-benchmark-report.json`，其中明确记录：

- `release_scope=diagnosis_only`；
- `label_authority=upstream_programmatic_scorer`；
- `human_validation_claimed=false`；
- `forecast_eligible=false`；
- `network_used=false`、`model_used=false`；
- AgentDojo commit
  `089ed468cf3ed0322acc66b0211f26d9d90dbf60`；
- corpus SHA-256
  `0bd19f8c93475f2b877c95d87077a374420a6bc5352fafbc9fcbeeff0dfe1954`。

### SDK 与发布物预验证

| 门禁 | 结果 |
| --- | --- |
| protocol-types build/check | `PASS` |
| evidence-bundle build/check | `PASS` |
| evidence-bundle runtime tests | `20 / PASS` |
| checked dist smoke | `2 / PASS` |
| Python wheel + sdist | `PASS` |
| 全新 venv 安装 wheel | `PASS`；确认从 site-packages 加载 |
| 安装态 Risk CLI | `PASS`；compound fixture 三因子均为 `FAIL`，且都有直接证据 |
| 安装态 Risk Schema | `3/3 PASS` |

## 3. 与规划的差异点及原因

1. **不再把人工黄金集作为本次发布前置条件。** 原方案成本高，用户明确要求
   复用现有优质数据。最终用 AgentDojo 的程序化标签建立公共门禁，同时诚实
   声明没有人类验证。
2. **合成集降为回归集。** 它仍非常适合发现状态语义、证据引用和宿主适配器
   被代码改坏，但不能证明外部效度，所以从发布证据中降权。
3. **监控规避没有强行补标签。** AgentDojo 没有对应权威标签，最终输出
   `NOT_APPLICABLE`；若硬从提示或攻击类型推断会破坏 Evidence First 边界。
4. **Forecast 从“满足校准可晋级”改为硬冻结研究态。** 公共攻击成功标签适合
   验证当前行为诊断，不等于未来风险 outcome。为了保证宣传、配置和代码行为
   一致，任何 calibration 都不能在当前版本中开启预测。
5. **真实业务对比结论被收窄。** 既有受控 shadow experiment 仍可证明新诊断
   在共享 fixture 上比声明的旧版/peer-inspired baseline 提供更多可引用信息；
   它不等于直接运行全部上游项目，也不支持“普遍优于同类”的宣传。
6. **没有发布 GitHub。** 这是用户明确要求的验收边界，不是实现阻塞。

## 4. 剩余风险

- 公共门目前只有 9 个精选案例、2 个 AgentDojo suite、1 个模型输出，存在样本
  窄、选择偏差和规则对固定样本过拟合的风险；指标不能外推到所有 Agent。
- 公共 corpus 验证的是由上游运行派生的 action/outcome summary，不是 AET 对
  AgentDojo 原始日志格式的直接适配能力。
- AgentDojo 的 `security=true` 表示注入任务成功，不是通用“安全”标签；AET
  runner 已显式转换语义，但维护者仍需避免口头误读。
- 监控规避尚无选定的公开、许可清晰、程序化标签数据，因此该因子的正式外部
  效度仍为 `UNKNOWN`，不能用另外两个因子代替。
- Risk Policy 仍需部署方提供正确的高影响工具、资产和监控面映射。策略漏项
  会产生 `UNKNOWN` 或漏覆盖，不能解释为安全。
- 诊断说明一次运行中观察到的行为，不证明内部动机、稳定人格、模型通用能力
  或 loss-of-control 概率。
- Forecast 要离开研究态，至少需要可合法复用、独立 outcome 标签的数据；按
  时间/仓库/宿主隔离的 holdout；无数据泄漏；足够独立样本和正例支持；低误报
  与校准门同时通过；并以代码评审解除 `research_only`。只调阈值或复用这 9 个
  发布案例都不满足条件。
- 当前工作树还包含用户此前的其他未提交修改。正式发布前应在用户验收后隔离
  发布分支、重跑 exact-tag CI、核对构建哈希，再由用户授权发布。

## 5. 最终状态与恢复点

本次开发状态是：**诊断发布候选完成，预测研究实现保留但不可晋级，人工工作
不作为门禁，GitHub 发布未执行。**

用户验收前不做 commit/tag/push。若继续扩充诊断公共门，优先增加许可清晰、
程序化标签、覆盖 monitoring surface 的公开轨迹数据；若找不到，保持
`NOT_APPLICABLE` / `UNKNOWN`，不新增人工标注义务。

---

# AET Review Graph 图优先审查交接执行报告

日期：2026-08-08

范围：按既定 Phase 0–5 落地代码结构图、证据图与 Improvement Constraint 的
组合图；默认 Agent 输入改为受限根切片；Mermaid 保留为人类投影；旧的
`agent-context.json` / `agent-task.md` 仅显式兼容导出。没有 commit、tag、
push、GitHub Release 或包发布。

## 1. 实际完成情况 vs 规划

| Phase | 规划 | 实际完成 | 状态 |
| --- | --- | --- | --- |
| Phase 0 | 冻结边界、基线和来源加载方式 | 确认 Evidence First、四态状态、无整体分数、只读默认；发现 `uv run --no-editable` 会加载旧 site-packages，统一改用 `PYTHONPATH=src` 当前源码门禁 | 完成 |
| Phase 1 | Review Graph 契约、Schema、验证器 | 新增 Code Graph、Review Graph、Slice、Manifest 四份 Schema；稳定 ID、规范 JSON、预算、引用、完整性和语义验证 | 完成 |
| Phase 2 | 代码结构图 | 用标准库 AST + Git 构建 file/class/function/method/test 节点与 CONTAINS/IMPORTS/CALLS/TESTS/INHERITS/MAY_CALL；动态目标保持 `UNKNOWN` | 完成 |
| Phase 3 | 证据、代码、约束合图与切片 | 将 Claim/Evidence/Limitation、Intent、Issue、允许/保护范围、验证和停止条件连接到变更文件、函数和测试；实现根切片、单跳展开和 stale 停止切片 | 完成 |
| Phase 4 | Package、CLI、MCP、投影、兼容 | 非覆盖、原子写入、SHA-256 完整性 Package；新增 `review-graph build/validate/open/expand/export-compat`；新增两个只读 MCP 工具；生成人类改进提示词和三张 Mermaid | 完成 |
| Phase 5 | 真实业务对照与优化 | 在 AET 空工具结果案例上执行静态上下文对照及隔离 Codex A/B/C；根据实际失败项两轮压缩并恢复 Limitation、Finding/Evidence ID；最终新方案业务准则 8/8 | 完成，初步观察 |

默认 Agent 消费路径为：

```text
用户 Intent + Improvement Constraint + Bundle + 当前 Git diff
  → aet review-graph build
  → review/root.slice.json
  → 审查 Agent 先读根切片
  → 仅在需要时通过 open/expand 查询一跳
  → 输出目标文件、证据化问题、保护范围、干预建议、验证和停止条件
```

最终实现没有要求 Agent 默认读取完整 `review/graph.json`，也不要求读取
`agent-context.json` 或 `agent-task.md`。这两个旧文件仅在运行
`aet review-graph export-compat` 时生成；快照过期时兼容导出会拒绝执行。

关键新增文件：

- `src/aet/review_graph/model.py`：协议常量、预算、稳定 ID、规范 JSON 和哈希；
- `errors.py`：稳定错误码；
- `indexer.py`：Git 绑定的 Python 静态结构索引；
- `builder.py`：代码、证据和控制节点合图；
- `slicer.py`：根切片、单跳展开、stale 停止切片；
- `validator.py`：结构、引用、状态、安全内核和 manifest 语义验证；
- `storage.py`：Package 构建、加载、哈希校验、快照校验和兼容导出；
- `render.py`：人类改进提示词、Mermaid 和兼容 Markdown；
- `schemas/review-graph/v1/*.schema.json`：四份协议 Schema；
- `tests/review_graph/`：契约、索引、合图、切片、存储、CLI 和 MCP 测试；
- `docs/review-graph.md`：使用流程、目录、输入输出、错误处理和业务观察。

## 2. 测试结果

### Phase 门禁

| 门禁 | 结果 |
| --- | --- |
| Phase 0 当前源码基线 | `635 tests / OK` |
| Phase 1 契约与 Schema | `6 tests / OK` |
| Phase 2 累计 Review Graph | `10 tests / OK` |
| Phase 3 累计 Review Graph | `16 tests / OK` |
| Phase 4/5 最终 Review Graph | `21 tests / OK` |
| MCP、Portable CLI、原 Improvement 集成 | `22 tests / OK` |
| README/增长门禁 | `4 tests / OK` |
| 全仓当前源码最终回归 | `656 tests / OK`，72.790 秒 |

最终全仓命令：

```bash
PYTHONPATH=src uv run --no-editable python -m unittest discover -s tests
```

覆盖的关键边界包括：重复或悬空 ID、缺失引用、路径越界、受保护路径变化、
Python 解析失败、动态调用、文件/字节/节点/边/上下文预算、Package 篡改、额外
文件、符号链接、非覆盖写入、快照 stale、明确兼容导出、CLI 和 MCP 有界参数。

### AET 真实业务样例

样例：`examples/evidence-grounded-improvement` 的空工具结果适配器。

静态输入结果：

- 根切片：12 节点、13 关系、实际落盘 6,505 字节；
- 旧 `agent-task + claim-chain Mermaid + improvement-chain Mermaid`：6,522 字节；
- 旧方式同等诊断所需的最小原始 Bundle/Improvement/源码材料：8,468 字节；
- 相对原始最小材料减少 23.2%；相对完整 Evidence Graph 的 76,348 字节减少
  91.5%；
- 根切片覆盖 11/11 必需语义：Intent、Issue、允许/保护范围、验证、停止条件、
  Claim、Evidence、文件、函数和测试；
- 纯代码结构图包含 0/5 授权安全控制，不能单独替代 Review Graph。

隔离、只读 Codex `gpt-5.6-sol` 单次 A/B/C 观察：

| 输入 | 8 项业务准则 | 观察 |
| --- | ---: | --- |
| 旧 Agent Task + Mermaid | 7/8 | 找对目标和修复，但把测试/上下文文件扩大为保护范围，并漏掉禁止行为 |
| 纯代码结构图 | 1/8 | 找到空结果 bug，但把测试列为目标，缺实际保护范围、Claim/Evidence、精确命令和整体限制 |
| 最终 Review Graph 根切片 | 8/8 | 只授权 `tool_result.py`，完整保留保护范围、禁止行为、Claim/Evidence、验证、停止条件和不可外推限制 |

8 项准则分别为：精确允许目标、精确保护/禁止范围、直接干预行为、精确验证命令、
三类停止条件、Claim 与 Evidence 引用、不可外推/非整体分数限制，以及不把测试
误当成可改目标。该实验只是一例、每组一次，不构成统计显著性或普遍优于所有
同类工具的结论。

## 3. 与规划的差异点及原因

1. **没有让 Agent 只读两张 Mermaid。** Mermaid 样式、重复标签和显示截断不适合
   作为规范机器输入，也没有快照/完整性门。最终保留 Mermaid 给人类，Agent
   默认读取由同一图派生的紧凑 JSON 根切片。
2. **没有删除完整 JSON 图。** `review/graph.json` 是单跳展开和审计的后端数据，
   但不进入默认上下文；这同时满足可追溯和省上下文。
3. **兼容文件没有完全移除。** 为旧宿主保留显式 `export-compat`，但默认 Package
   不含这两个文件，且 stale 时拒绝导出。
4. **关键 Limitation 从纯展开层回到根切片。** 第一轮真实 Agent 观察漏掉“不可
   外推生产仓库/不能形成整体信任分数”。最终把直接关联 Claim/Evidence 的限制
   以紧凑字段放进规范节点，完整限制节点仍可展开。
5. **根切片改为紧凑 JSON 落盘。** 第一版报告按规范 JSON 计算 6.4 KB，但实际
   文件因显示缩进接近 8 KB；最终交付文件与预算计算使用同一规范字节，避免
   指标和 Agent 实际读取不一致。
6. **代码索引限于 Python 标准库 AST。** 这是规划内 Phase 1 的最小可行范围，
   没有引入 Tree-sitter、数据库或网络依赖。其他语言保持未实现，不伪造覆盖。
7. **当前混合脏工作区不会强行构建本 Issue 的可执行根切片。** 以 `v1.15.0`
   对比当前 AET 工作树时发现 202 个范围外 Python 变更、216 个必需节点，系统
   返回 `context_limit`/FAIL，而不是丢弃越权变更。这是预期 fail-closed。
8. **没有正式发布。** 用户要求先验收；同时工作树含大量任务前用户修改。没有
   commit、tag、push、GitHub Release、PyPI 或 npm 发布。

## 4. 剩余风险

- 真实 Agent 对照只有一个 AET 样例、每组一次；尚不能证明跨仓库、跨语言、
  跨模型的稳定提升。正式对外宣称普遍提升前，应复用公开冻结 bug 数据，按仓库
  与宿主隔离 holdout，并运行多次观察。
- 代码索引当前只理解 Python AST。JavaScript/TypeScript、Java、Go、Rust 等会
  缺少结构图；在新增对应确定性索引器前必须报告 `UNKNOWN`，不能用文件名猜测
  确定调用关系。
- Python 的反射、动态 import、猴子补丁、依赖注入和运行时分派不能由 AST 完整
  证明；当前只用 `MAY_CALL`/`UNKNOWN` 暴露这项限制。
- Bundle 与 Improvement 的质量仍决定诊断上限。缺失、过期或错误授权会阻断，
  但 AET 不能自动替代人类确认 Intent 和约束是否写对。
- 默认 8 KB/24 节点/32 关系预算在大型跨模块变更上可能触发 `context_limit`。
  这是安全失败，不是产品完成；调用方可显式提高预算，但不应静默截断必需节点。
- 当前 Package 写入在同一父目录内通过临时目录原子改名；它不能提供跨文件系统
  或断电条件下的分布式事务保证。
- 本次只验证源码运行，没有构建新的 wheel/sdist 或隔离安装测试，因为用户尚未
  授权发布，且当前工作树包含其他未完成功能。发布前需在隔离分支重跑包内容、
  安装态 Schema、CLI/MCP smoke、exact-tag CI 和哈希门禁。
- 正式 GitHub 发布仍待用户验收与明确授权。

## 最终状态与恢复点

Review Graph 实现、文档、测试和初步业务观察已完成。默认 Agent 输入现在是
`review/root.slice.json`；Mermaid 和人类改进提示词继续生成；兼容文件仅显式
导出。下一步是用户验收，不执行发布动作。

---

# AET v1.19.0 Evidence Plane 发布执行报告

日期：2026-08-08

## 1. 本次发布把 AET 讲清楚了什么

AET 现在统一定义为编程 Agent 的本地 Evidence Plane：把 Intent、Git 状态、
命令、产物、运行轨迹与审查约束编译成哈希绑定、可验证、可裁剪、可移交的证据。
它不是另一个 Agent，不替代测试或 CI，也不获得编辑、提交、推送、合并、发布或
自动干预 authority。

README 已围绕这一定义重写，并补齐静态/动态流程、完整能力矩阵、安装路径、
案例库、双语架构动画与 30 秒介绍视频。公共 PyPI 的真实最新版本是 v1.11.1，
因此 v1.19.0 使用精确 GitHub Release wheel，不把尚未发生的 PyPI 发布写成事实。

## 2. 容易理解的生产形态案例

新增 Refresh Token 并发轮换竞态：事件工单、日志、diff 与 proof 分散时，人只能
审批 Agent 的不透明总结，Agent 又需要重读大段仓库并可能触碰签名/配置。AET
把 Intent、事件 Evidence、代码关系、保护范围与 Proof Freshness 绑定在同一
Review Package 上，再产生两种同源投影：

- 给人：动态 Mermaid 显示“撤销已成功、数据库提交超时且状态 UNKNOWN”的失败
  窗口，所以当前不能把上线写成 PASS；
- 给 Agent：仅给两个允许文件、一条回归测试、Evidence ID、保护路径与停止规则；
- 快照、证据或授权漂移时停止，不继续使用旧上下文。

这是冻结的 production-shaped case，不冒充客户事故，也不把动态图等同于运行时
遥测或上线 authority。

## 3. 相比旧交接与 code-review-graph 的边界

冻结案例中，v1.19 Review Graph 根切片为 6,505 bytes / 12 nodes / 13 edges；
等价最小原始材料为 8,468 bytes，减少 23.2%；旧 Agent Task 加两张 Mermaid 为
6,522 bytes。该结果仅说明这一冻结输入的首次读取字节，不外推为普遍 Token 或
模型质量提升。`code-review-graph` 的 Tree-sitter、多语言和增量结构索引更广；
AET 当前限于 Python AST，但把 Evidence、authority、protected scope、proof
freshness 与 fail-closed stop condition 纳入同一审查图。

## 4. 本地发布门禁

| 门禁 | 结果 |
| --- | --- |
| 当前源码全回归 | `657 tests / OK` |
| Behavioural Risk 公共诊断门 | `9 cases / 27 labels / PASS`；exact-case accuracy、precision、recall、cited-failure coverage 均 `1.0`，FPR `0.0` |
| TypeScript protocol-types / evidence-bundle | build、check 与 runtime/dist tests 全部 `PASS` |
| README | 213/250 行预算、6 文件本地链接、严格 launch readiness 全部 `PASS` |
| Mermaid | 英中 README 与英中完整案例共 6 块，Mermaid 11.16 解析 `PASS` |
| 媒体 | 16 个双语产物哈希绑定；SVG/PNG/GIF/30 秒 H.264 视频与 motion report `PASS` |
| AET strict audit | `PASS`，无 FAIL/UNKNOWN |
| Wheel / sdist | v1.19.0 构建成功；Review Graph、Risk 代码与 7 份 Schema 均在 wheel 内 |
| 隔离安装 | `aet --version`、stale-proof Demo、Review Graph/Risk CLI smoke 全部 `PASS` |

远端 exact-commit CI、Release Workflow 和 GitHub Release 资产需要在本提交推送、
打 tag 后产生，不能预写尚不存在的 run ID。PyPI 发布不在本次授权范围内。
