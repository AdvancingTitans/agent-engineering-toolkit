# AET v1.16.0 执行报告

日期：2026-07-29
规划来源：Codex 侧边浏览器中的 GPT-5.6 Sol Pro 完整规划
基线版本：`v1.15.0`
发布版本：`v1.16.0`

## 实际完成情况 vs 规划

| Phase | 规划目标 | 实际完成情况 | 状态 |
|---|---|---|---|
| Phase 0 | 建立 Improvement Layer 与 `doctor`，不修改 Bundle/Evidence/Finding schema | 新增 `src/aet/improvement/`；严格加载 Bundle、校验 Evidence 引用，并以现有 Claim 作为 Finding-compatible 输入 | 完成 |
| Phase 1 | 确定性 Finding → Issue → Constraint → Human Report | 实现归一化、聚合、去重、优先级、最多 5 条、稳定 JSON/Markdown；`aet improve <bundle>` 生成可审查的人类报告 | 完成 |
| Phase 2 | Candidate、约束提示词与 fail-closed 验证 | 实现 `PROPOSED` Candidate、允许路径、验证命令、停止条件、grounding/scope/reference/strength/anti-gaming 校验 | 完成 |
| Phase 3 | Proof-bound Verification、Before/After 与 Outcome | 实现 `verify`、`compare`；只有真实 changed paths、当前且匹配的 PASS Proof 和无回归比较才能成为 `verified_improvement` | 完成 |
| Phase 4 | 非阻断 PR Improvement Summary | 新增 `.github/workflows/aet-improvement.yml`；无 LLM、明确非权威、生成失败不阻断 merge | 完成 |
| Phase 5 | Evidence Atlas 的 Improvement Chain / Regression Lineage | 新增两个固定 Perspective，Python Schema、MCP、TypeScript SDK 与 Viewer 约定同步为十个 Perspective | 完成 |
| 实际案例 | 同一证据同时生成人类改进提示与可视化证据图谱 | 新增可复现 empty-tool-result 案例、Human Report、Agent Task、Claim Chain、Improvement Chain、JSON 产物及集成测试 | 完成 |
| README 与媒体 | 更新产品介绍、使用方式、静态/动态架构、流程与中英视频 | 英中 README、架构文档、产品全景、工作流 GIF、案例 GIF/PNG/SVG、Atlas 架构图及两组 30 秒中英 MP4 全部更新 | 完成 |
| 发布 | 发布 GitHub 最新 Release | 精确标签 CI 与受保护 Release workflow 成功，GitHub Release `v1.16.0` 已公开 | 完成 |

## 实际案例：证据、提示词与证据图谱

案例模拟现实请求：“请 Agent 审查项目并告诉我该怎么改。”

示例中的缺陷适配器把空工具结果错误地表述成“没有发现安全问题”。回归命令真实退出 `1`，AET 将失败记录为 counter-evidence `ev-empty-result-regression`，再从同一份 Portable Evidence Bundle 产生：

- 人类报告：`IMP-001 · P1_HIGH · unsupported_claim`
- 有界 Agent Task：状态始终为 `PROPOSED`
- 精确允许路径：仅 `examples/evidence-grounded-improvement/sample_project/tool_result.py`
- 精确验证命令：`python examples/evidence-grounded-improvement/sample_project/test_tool_result.py`
- Claim Chain：显示 unsupported Claim 与已重现的失败证据
- Improvement Chain：保持 `UNKNOWN`，因为 Bundle v1 尚无独立 Improvement/Outcome 记录

复现命令：

```bash
uv run python examples/evidence-grounded-improvement/build_example.py \
  --output .aet/evidence/empty-result-review
uv run aet improvement doctor .aet/evidence/empty-result-review
uv run aet improve .aet/evidence/empty-result-review
uv run aet improve prompt IMP-001
uv run aet atlas build .aet/evidence/empty-result-review \
  --perspectives claim-chain,conflicts,improvement-chain --no-llm
```

两类产物是同一权威 Bundle 的兄弟投影：

```text
Portable Evidence Bundle
├── Improvement Analyzer → Human Report + PROPOSED Agent Task
└── Canonical Evidence Graph → Evidence Atlas
```

它们通过稳定 Evidence ID 相互对应，但不形成权限回路：提示词不会写回成为 Evidence，Atlas 也不会把 Recommendation 升格为事实。

已跟踪案例：

- `examples/evidence-grounded-improvement/`
- `examples/evidence-grounded-improvement/artifacts/human-report.md`
- `examples/evidence-grounded-improvement/artifacts/agent-task.md`
- `examples/evidence-grounded-improvement/artifacts/claim-chain.mmd`
- `examples/evidence-grounded-improvement/artifacts/improvement-chain.mmd`

## README、架构与媒体

- 英中 README 已更新产品定位、六个核心问题、CLI 使用方式、真实案例、权威边界、静态/动态架构、流程图和高级文档入口。
- Evidence Atlas 从八个固定 Perspective 扩展为十个；unsupported Claim 使用红色虚线语义，避免与 supported Claim 混淆。
- 新增双语案例 SVG、PNG、GIF 和完整 SHA-256 Manifest。
- 重制双语 Quick workflow GIF、项目全景、Atlas 架构图、产品介绍视频和 Atlas 介绍视频。
- 两组介绍视频均为 1600×900、30 秒、H.264、无音轨；README 明确标记为 silent，未虚构旁白。
- Quick workflow 动画为 960×700、115 帧、20 fps、5.75 秒；全部帧唯一，循环接缝、几何、构图、marker 与 XML 检查通过。
- 依据 `fireworks-tech-graph` 视觉规范完成几何/构图验证、semantic motion 报告、contact sheet 与逐图人工检查；曾发现并修正 Atlas 图文字溢出及英文视频中的中文混排。

## 测试结果

| 验证 | 结果 |
|---|---|
| `python -m unittest discover -s tests` | 478 项通过，81.500 秒 |
| GitHub CI 使用的 pytest | 488 passed，636 subtests passed，79.06 秒 |
| Improvement 单元/集成/真实案例 | PASS |
| TypeScript SDK | 16/16 PASS |
| TypeScript dist smoke | 1/1 PASS |
| TypeScript compatibility guard | PASS |
| Atlas Viewer / Mermaid 11.16.0 | PASS |
| stale-proof demo | `EXACT_MATCH` → `RELEVANT_FILES_CHANGED`，PASS |
| core / validation / held-out / deterministic suites | 全部 PASS |
| `aet audit . --strict` | PASS |
| sdist / wheel 构建 | PASS |
| 隔离 wheel 版本烟测 | `1.16.0`，PASS |
| Release classification | deterministic，45 个敏感路径全部绑定确定性测试，PASS |
| `git diff --check` | PASS |
| 标签 CI | Run `30420607956`，PASS |
| GitHub Release workflow | Run `30420768704`，PASS |

实现期间还修复了以下被测试捕获的问题：

1. Prompt 最初只使用正向 `evidence_refs`，导致 unsupported Claim 无法引用真正的 counter-evidence；现已合并两类引用。
2. Atlas 最初把 unsupported Claim 套用 supported 样式；现已增加独立红色虚线样式与回归测试。
3. README/SDK/MCP 的历史“八个 Perspective”断言已统一为十个。
4. 媒体生成过程中发现 Atlas 架构图文字溢出和英文视频混入中文，均在最终产物前修正并重新验证。

## 与规划的差异点及原因

1. **Bundle v1 没有独立 Finding collection。**
   为遵守“不修改 Bundle/Evidence/Finding schema”，实现只把已通过严格 Bundle 校验的 Claim 适配为 Finding-compatible 输入，没有扩展正式 Bundle v1。

2. **两个 Improvement Atlas Perspective 当前通常为 `UNKNOWN`。**
   Bundle v1 没有独立 Issue、Constraint、Candidate、Verification Contract、Outcome 或 Regression Fixture collection。实现没有从文本猜测这些对象；缺少来源绑定时明确输出 `UNKNOWN`。

3. **未创建规划中仅作为“推荐打包”的第二套源码根目录。**
   实际文件按仓库既有 `src/`、`tests/`、`docs/`、`.github/` 布局落地，避免复制实现和形成双重真相源。

4. **未声称真实 Agent 的 `pass@1`、`pass^3` 或用户理解率达标。**
   规划未提供冻结任务集、模型版本、预算和人工研究样本。这些指标保持未测量，而不是用确定性单测替代真实行为证据。

5. **发布 dispatch 从 API 改为已登录 GitHub UI。**
   本地 PAT 可读取 CI 与产物，但无 Actions 写权限，REST dispatch 返回 403。随后使用用户已授权、已登录的 GitHub UI 触发同一个 `release.yml`；所有 tag、CI、artifact 和分类 Gate 保持不变，没有直接绕过工作流创建 Release。

6. **媒体渲染后端使用 Chrome/ffmpeg/sips。**
   本机 Cairo 动态库不可用，因此改用现有渲染后端；最终 SVG/PNG/GIF/MP4 均通过尺寸、语义、hash 与人工视觉检查。

## 剩余风险

- SC-001“90% 用户可理解”仍需真实用户研究。
- Codex/Claude Code 的 `pass@1`、`pass^3`、Scope Violation Rate 仍为 `UNKNOWN`。
- PR Improvement Summary 已通过契约测试，但尚未在真实 Pull Request 中积累权限、评论更新和分支保护的运行样本。
- Bundle v1 无原生 Improvement collection；完整生命周期跨机器传递仍依赖 `.aet/improvements/` 伴随文件。
- Symbol 级 Reference Validator 当前只理解 Python AST；其他语言会 fail closed 为 `INVALID_REFERENCE`。
- 当前案例证明确定性 grounding、提示词/图谱同源与 fail-closed 行为，不代表所有仓库或所有模型上的总体改进效果。

## 发布结果

- Release：<https://github.com/AdvancingTitans/agent-engineering-toolkit/releases/tag/v1.16.0>
- 发布提交：`bfe062a9f68b805a5b629f32828510a411c9a1f9`
- 标签 CI：`30420607956`
- Release workflow：`30420768704`
- Release ID：`361498297`
- CI release-candidate artifact：`8711774368`
- 资产：wheel、sdist、`manifest.json`、`release-classification.json`、`classification-verification.json`、`release-evidence.json`
- Release class：`deterministic`
- Real Host Gate：`NOT_APPLICABLE`
- PyPI：未发布

## 结论

规划中的 Phase 0–5、真实应用案例、README 全面更新、双语静态/动态媒体与 GitHub Release 均已完成。最终实现保持 Evidence First 边界：证据是唯一事实来源，提示词与图谱只是可追溯投影，Candidate 不自动获得执行权限，缺失证据继续显示为 `UNKNOWN`。
